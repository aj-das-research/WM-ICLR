\section{Analysis}
\label{sec:analysis}

\begin{figure}[t]
  \centering
  \figorpending{interpret.pdf}{5.0cm}{Where and why ShiftWM wins: win maps, causal knockout, motion deciles, action steering.}
  \caption{\textbf{Where and why \ours{} wins (held-out DROID, $k{=}10$).} (a) Per-patch error of Direct minus \ours{} (green:
  \ours{} lower), with a zoom on the moving region and the learned gate. (b) Removing transport at test time. (c) Gain over
  Direct per motion decile. (d) Change of the transport field under counterfactual actions.}
  \label{fig:qualitative}
\end{figure}

\begin{figure}[t]
  \centering
  \figorpending{mechanism.pdf}{5.2cm}{How \ours{} works: forecasts in feature space, the additive decomposition, and sharpness.}
  \caption{\textbf{Inside \ours{}.} (a) Held-out forecasts at $k{=}10$ in feature space. (b) The forecast is the sum of a
  kept, a moved and a corrected term. (c) Transport keeps more spatial contrast than regression (95\% CIs).}
  \label{fig:mechanism}
\end{figure}

\paragraph{How does \ours{} compose a forecast?} \Cref{fig:mechanism}(a) projects the $16{\times}16{\times}C$ feature grids
to colour with a shared PCA: the \ours{} forecast preserves the layout of the true future, while Direct and AR drift
toward a smoother grid. Because \cref{eq:output} is additive, each forecast splits exactly into the kept term
$(1-g)\Z_0$, the moved term $g\mathbf{T}$ and the correction $\mathbf{r}$ (\cref{fig:mechanism}b): the static scene
is carried by the kept term, the moving arm by transport, and the correction concentrates on newly revealed content.
\Cref{fig:mechanism}(c) quantifies this over all DROID test windows as the spatial variance of the forecast relative
to the true grid. Regression toward the conditional mean loses contrast as the horizon grows; transport copies measured
features and keeps more of it (0.80 vs.\ 0.77 for Direct and 0.74 for AR at $k{=}10$).

\paragraph{Causal role of transport.} We intervene on the trained model (\cref{fig:qualitative}). Setting the gate to
zero at test time, so that each forecast is $\Z_0+\mathbf{r}$, raises the error by \good{\koMoving\%} on moving patches and
by \koHighGate\% on patches with a high learned gate, but only by \koStatic\% on static patches: the transport pathway, not
the correction, carries the prediction of moving content. Replacing the future actions with those of another episode
changes the transport field \good{\steerRatio$\times$} more on moving than on static patches, so the actions steer
\emph{where} features are moved. Across all ten deciles of true motion, \ours{} has lower error than Direct
(\good{+\decileMin\% to +\decileMax\%}), and the win maps show the gains concentrated on the arm and gripper.

\paragraph{What does the transport field learn? (Q4)} We convert $\pi_{k,i}$ into an expected source
displacement per patch and compare it, at patch resolution, with optical flow estimated by RAFT
\citep{teed2020raft} between the observed and future frames (\cref{fig:qualitative}). In \cref{fig:qualitative}, transport concentrates
on the moving arm, gripper and instruments; the static background keeps the identity path, and newly revealed
regions fall back to the correction term. \pend{}

\input{submission_folder/tables/regions.tex}
\paragraph{Where do the gains come from?} We split every test window into \emph{moving} patches (the 25\% with the
largest true feature change, a property of the data shared by all methods) and \emph{static} patches
(\cref{tab:regions}). Two effects add up. On moving patches, transport predicts where content goes and \ours{}
has the lowest error. On static patches, every other learned predictor is \emph{worse than copying}: regenerating
the grid corrupts background that should not change, while \ours{}'s identity path keeps it intact and is the only
learned model below persistence. Together, \ours{} removes more of the persistence error than any baseline.

\begin{figure}[t]
  \centering
  \begin{minipage}[t]{0.49\linewidth}\centering
    \figorpending{tradeoff.pdf}{3.9cm}{Forecast error (x) vs.\ action ranking accuracy (y) for every
    predictor and ablation; marker size = planning success.}
  \end{minipage}\hfill
  \begin{minipage}[t]{0.49\linewidth}\centering
    \figorpending{gain_vs_motion.pdf}{3.9cm}{Relative MSE gain of \ours{} over Direct, binned by true
    per-patch feature change, DROID and Hamlyn test.}
  \end{minipage}
  \caption{\textbf{Left:} forecast error vs.\ action-ranking accuracy (marker size: planning success). \textbf{Right:} gain of \ours{} over Direct by amount of motion.}
  \label{fig:tradeoff}
\end{figure}

\paragraph{Accuracy versus action sensitivity.} A forecast that stays close to $\Z_0$ can score
well on average while barely responding to the commanded actions, and a planner then cannot rank
candidate action sequences. We measure this directly as the rate at which true future actions yield
lower error than actions from another episode (\cref{fig:tradeoff}, left). Anchored predictors can reach low
open-loop error while reacting weakly to actions; the action-contrastive objective moves \ours{} toward high
accuracy \emph{and} high sensitivity. \pend{}

\input{submission_folder/tables/efficiency.tex}
\paragraph{Efficiency.} \Cref{tab:efficiency} compares parameters, compute per forecast step and
latency. Transport adds \pend{} parameters. Because \ours{} and Direct decode all $K$ steps in
parallel, a $K$-step forecast needs one encoder pass, whereas recursive predictors need $K$
(\pend{}).
