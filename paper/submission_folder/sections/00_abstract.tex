\begin{abstract}
World models let an agent evaluate actions before executing them, and forecasting in the patch-feature space of a
frozen visual foundation model keeps them compact.
%
Current latent world models, such as DINO-WM and V-JEPA 2-AC, regenerate the whole feature grid at every future step
and feed their predictions back as inputs.
%
Yet in manipulation and surgical scenes most content persists and only moves, so regeneration corrupts the static
background and lets errors compound.
%
We introduce \ours{}, a latent world model that forecasts by \emph{moving what it has already seen}: for every future
step it predicts an action-conditioned soft transport of nearby observed patch features, learned without flow
supervision, a gate between moved and kept features and a correction for new content, and it decodes all horizons in
parallel.
%
As an output head, it also attaches to open-source world models.
%
On held-out real-robot video from DROID, Language-Table and the bimanual IWS tasks, and on seven surgical tasks from
Open-H, \ours{} has the lowest error of all matched output heads, \droidVsDirect\% below a direct predictor on DROID;
as a plug-in, it lowers the error of the 1.3B-parameter V-JEPA 2-AC by \vjepaMSERed\%.
\end{abstract}
% Teaser figure: generated by fig_teaser() in paper/submission_folder/figures/src/make_figures.py from completed runs.
\begin{figure}[h!]
  \centering
  \includegraphics[width=\linewidth]{teaser.pdf}
  \caption{\textbf{\ours{} moves what it has seen instead of regenerating it.} (a) One held-out DROID window
  (\cref{app:qualitative}); Direct, a matched regression head, rewrites every patch, whereas \ours{} moves the arm's
  patches (arrows; dashed: the 25\% that change most). (b) Error vs.\ persistence. (c) \% lower error than the best learned baseline on every
  benchmark; plug-ins: vs.\ the same backbone without the head.}
  \label{fig:teaser}
\end{figure}
