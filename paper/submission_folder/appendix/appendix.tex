% ============================================================== Appendix
\paragraph{Appendix overview.} The appendix follows the order of the main paper: the problem setting and extended
related work (\cref{app:background}); architecture, proofs and plug-in heads (\cref{app:method}); benchmarks,
data, baselines, training and statistical procedures (\cref{app:exp}); per-horizon results, ablations, planning and
efficiency (\cref{app:results}); and mechanism analyses, qualitative rollouts, failure cases and discussion
(\cref{app:analysis-all}).

\section{Background and problem setting}\label{app:background}
\subsection{The task and why it matters}\label{app:setting}
\paragraph{Task.} An action-conditioned latent world model receives the last $H$ observed frames of a scene, encoded by a
frozen visual foundation model into grids of patch features, the actions applied between them, and a candidate sequence
of $K$ future actions. It must predict the patch-feature grids of the next $K$ frames, without seeing them. It never
generates pixels: the forecast lives in the same feature space that downstream consumers read.
\paragraph{Why it is useful.} Such forecasts serve three uses. (i) \emph{Planning}: a model-predictive planner samples
many candidate action sequences, forecasts each, and executes the one whose forecast is closest to a goal
\citep{zhou2025dinowm,maes2026lewm}; forecast accuracy and sensitivity to the actions decide which candidate wins.
(ii) \emph{Control and policy evaluation}: a world model can score a policy's proposed actions before they are executed
on a real robot. (iii) \emph{Anticipation}: in surgical robotics, forecasting where instruments and tissue will be a few
seconds ahead is a building block for assistance and safety monitoring. All three need forecasts that are accurate many
steps ahead, respond to the actions, and hold on scenes not seen in training.
\paragraph{An example.}
\begin{figure}[h]
  \centering
  \includegraphics[width=\linewidth]{setting.pdf}
  \caption{\textbf{The forecasting problem on real held-out episodes.} Three observed frames and the future actions are given; features at future steps are predicted. Right: true feature change over the $16{\times}16$ grid.}
  \label{fig:setting}
\end{figure}
\Cref{fig:setting} shows the task on two held-out episodes, one from DROID (a Franka arm in a real kitchen) and one from
the Open-H Hamlyn surgical tasks (a dVRK instrument over a training phantom). The world model receives the last $H{=}3$
frames $t{-}2,t{-}1,t$, encoded by a frozen DINOv2 encoder into $16{\times}16$ grids of 384-D patch features, the actions
applied between them, and a sequence of $K{=}10$ future action blocks (the commanded end-effector trajectory, bottom rows).
It must predict the feature grids $\Z_{t+1},\dots,\Z_{t+10}$, 3.3\,s into the future, without seeing any future frame.
The episodes are chosen by a fixed rule (largest feature change among test episodes; \cref{app:qualitative}).

The rightmost panels show where the features actually change between $t$ and $t{+}10$. Change is concentrated on the
robot and on the objects it touches: in these two windows a quarter of the patches carry more than 40\% of the total
change, while large parts of the scene change little. This is the structure that \ours{} exploits: most future
patch features are already present in the observed grid, displaced by motion, and only a small part is new content.

\subsection{Why existing world models fall short}\label{app:why-short}
Current latent world models regress the complete feature grid at every future step, and most roll out recursively
(\cref{sec:related}). Our matched baselines isolate these two design choices (\cref{sec:experiments}), and the held-out
results show three concrete failure modes.
\begin{enumerate}[leftmargin=1.4em,itemsep=1pt,topsep=2pt]
  \item \textbf{Regenerating static content corrupts it.} On static DROID patches, every learned regression predictor
  (Direct, AR, AR-TF) has higher error than simply copying the last observed frame (\cref{tab:regions}). A regression head
  must output exactly zero change there, and any approximation error damages background that should not change.
  \item \textbf{Regenerating moving content blurs it.} On moving patches, an additive head must write out the feature
  contrast along the motion (Proposition~\ref{prop:residual}). At $k{=}10$, Direct and AR retain less of the true spatial
  contrast than \ours{} (\cref{fig:mechanism}b), and \ours{} has \droidMovingVsDirect\% and \droidMovingVsAR\% lower
  moving-patch error than Direct and AR (\cref{tab:regions}).
  \item \textbf{Recursion compounds error.} Predictors that consume their own forecasts accumulate error with the horizon
  (Proposition~\ref{prop:compound}, \cref{fig:horizon}). The DINO-WM-style AR-TF, trained with teacher forcing, is worse
  than persistence on DROID once rolled out, and \ours{} has \droidVsAR\% lower error than the rollout-trained AR
  (\cref{tab:main}).
\end{enumerate}
At the same time, much of the future is already observed: on moving DROID patches, copying the single best observed
feature from the transport window removes \geoOracleRed\% of the error of staying put (\cref{fig:geometry}b). \ours{} is
designed around this structure: it keeps static content through an exact identity path, moves observed content through
transport, and decodes every horizon from measured features.

\subsection{Related formulations}\label{app:related-formulations}
Latent world models such as DINO-WM \citep{zhou2025dinowm}, V-JEPA 2-AC \citep{assran2025vjepa2} and LeWM
\citep{maes2026lewm} predict future features of a frozen or jointly trained encoder with a network whose output
\emph{replaces} the feature grid: absolute regression, usually rolled out one step at a time. Residual variants predict
$\Z_k-\Z_0$, which keeps static patches cheap but still requires the network to write the full displacement of
every moving patch (Proposition~\ref{prop:residual}). Video prediction in pixel space has long used explicit motion
(flow warping, dynamic filters, kernel-based transformations); \ours{} brings this structure to the feature space of
foundation models, where features are high-dimensional and semantically stable under motion, so that copying them is
accurate. Unlike optical-flow pipelines, the transport is never supervised with flow: it is learned end-to-end from the
feature forecasting loss, conditioned on future actions, and is used at every horizon directly from the observed grid.

\subsection{Extended related work}\label{app:related-extended}
\paragraph{Generative world models and model-based reinforcement learning.}
Pixel-space generative world models learn high-fidelity, action-controllable video simulators. NWM conditions a diffusion
transformer on navigation actions~\citep{bar2025nwm}; Cosmos and Cosmos-Predict2.5 provide large video foundation models
for physical AI~\citep{nvidia2025cosmos,nvidia2025cosmospredict25} that Cosmos Policy adapts for control~\citep{kim2026cosmospolicy};
Ctrl-World, IRASim, UniSim and IWS target robot manipulation and policy evaluation~\citep{guo2026ctrlworld,zhu2025irasim,yang2024unisim,wang2026iws};
and the Genie family generates interactive environments from video, image or text prompts~\citep{bruce2024genie,parkerholder2025genie3}.
Model-based reinforcement learning instead learns task-specific latent dynamics jointly with rewards and policies, from
PlaNet and DreamerV3 to TD-MPC2, IRIS and DIAMOND~\citep{hafner2019planet,hafner2025dreamerv3,hansen2024tdmpc2,micheli2023iris,alonso2024diamond}.
These systems optimise sample fidelity or online return with their own encoders and data, so they do not share our
contract of a fixed frozen encoder, identical observations and actions, and targets in common feature coordinates. We
therefore treat them as complementary rather than as compute-matched baselines.
\paragraph{World models for surgery and evaluation suites.}
Surgical world modelling is driven by generative video models: Endora simulates endoscopy video~\citep{li2024endora},
SurgWM learns latent-action-controllable surgical video~\citep{koju2025surgwm}, SurgVista targets long-horizon
instrument--tissue dynamics~\citep{pan2026surgvista}, SWoMo combines neural and symbolic components for cataract
surgery~\citep{sivakumar2026swomo}, and Cosmos-H-Surgical learns surgical robot policies from videos through world
modelling~\citep{he2025cosmoshsurgical}. Open-H-Embodiment releases paired video and kinematics across surgical platforms,
together with a kinematics-conditioned surgical simulator~\citep{openh2026embodiment}; SutureBot, JIGSAWS, Cholec80 and
CholecT50 provide surgical demonstrations and task, skill and workflow
annotations~\citep{haworth2025suturebot,gao2014jigsaws,twinanda2017endonet,nwoye2022rendezvous}, and SurgWMBench
benchmarks instrument-motion planning~\citep{liu2026surgwmbench}. We use Open-H dVRK recordings to test
kinematics-conditioned forecasting in feature space, a regime these pixel generators do not target. Robot datasets such
as DROID, BridgeData~V2 and Open X-Embodiment supply action-labelled video~\citep{khazatsky2024droid,walke2023bridgedata,oneill2024openx}.
WorldScore, WorldModelBench, EWMBench and Physics-IQ score generated video for controllability and physical
plausibility~\citep{duan2025worldscore,li2025worldmodelbench,yue2025ewmbench,motamed2026physicsiq}, whereas
stable-worldmodel standardises latent planning evaluation~\citep{maes2026stable}. We follow the latter for control and
report feature-space forecasting error for prediction.
\paragraph{Encoders.} \ours{} applies to any frozen patch encoder, e.g.\ DINOv2 or DINOv3~\citep{oquab2023dinov2,simeoni2025dinov3}.

\section{Extended method}\label{app:method}
\subsection{Architecture}\label{app:impl}
\paragraph{Architecture.} Width $d{=}384$, 6 heads, MLP ratio 4, memory encoder $L_e{=}4$ layers of
full self-attention over the $H\cdot N{=}768$ tokens of $H{=}3$ observed $16{\times}16$ grids of $C{=}384$-D DINOv2-S features. Query
decoder: $L_d{=}4$ layers, each with self-attention over the $N{=}256$ query tokens, cross-attention
to memory and an MLP, modulated by AdaLN-zero on $\mathbf{c}_k$. Action embedding: a 2-layer MLP
followed by a single-layer unidirectional GRU of width $d$. Transport: key dimension 64, window
$w{=}7$ over $S{=}3$ source frames (147 candidates per query), learned identity bias initialised at
$\beta{=}4$, and gate bias $-3$. Correction head: LayerNorm, then a zero-initialised linear map to $C$,
then a per-channel scale initialised at 1. Parameters: 21.75M (\ours{}) and 21.70M (Direct, AR, AR-TF).

\subsection{Proofs}\label{app:proofs}
\paragraph{Proof of Proposition~\ref{prop:bayes}.} The conditional mean minimises expected squared error. Since $\Z_0$
is part of the context and $\E[\bm{\varepsilon}_k\mid\mathbf{P}_k,\Z_0,\mathbf{a}]=\mathbf{0}$,
$\E[\Z_k\mid c]=\E[\mathbf{P}_k\mid c]\,\Z_0+\E\big[\E[\bm{\varepsilon}_k\mid\mathbf{P}_k,c]\mid c\big]=\bar{\mathbf{P}}_k\Z_0$.
Each row of $\mathbf{P}_k$ is a one-hot vector, so each row of $\bar{\mathbf{P}}_k$ is a probability vector supported
on the same indices, hence on $\mathcal{W}(i)$ for row $i$. For $g=1$ and $\mathbf{r}=\mathbf{0}$, \cref{eq:output}
gives $\hat{\Z}_{k,i}=\sum_{j\in\mathcal{W}(i)}\pi_{k,i}(j)\Z_{0,j}$. Softmax weights with logits
$\ell_j=\log\bar{P}_{k,ij}$ on the support and $\ell_j\to-\infty$ elsewhere converge to row $i$ of $\bar{\mathbf{P}}_k$,
so $\hat{\Z}_k\to\bar{\mathbf{P}}_k\Z_0$. With $S>1$ source frames the same argument applies to a motion model whose
rows select patches of any observed frame. $\square$

\paragraph{Proof of Proposition~\ref{prop:residual}.} An additive head outputs $\Z_0+\mathbf{R}$; matching
$\mathbf{P}\Z_0$ requires $\mathbf{R}=(\mathbf{P}-\mathbf{I})\Z_0$, whose $i$-th row is
$\mathbf{z}_{0,\pi(i)}-\mathbf{z}_{0,i}$; summing squared row norms gives the claim. For \ours{}, the one-hot
transport onto $\pi(i)$ (the limit above) with $g=1$ reproduces $\mathbf{P}\Z_0$ with $\mathbf{r}=\mathbf{0}$. $\square$

\paragraph{Proof of Proposition~\ref{prop:compound}.} Let $e_j=\|\hat{\Z}_j-\Z_j\|$ for the recursive rollout
$\hat{\Z}_j=f(\hat{\Z}_{j-1})$ and $\Z_j=f(\Z_{j-1})+\bm{\eta}_j$ with $\|\bm{\eta}_j\|\le\delta$. Then
$e_j\le\|f(\hat{\Z}_{j-1})-f(\Z_{j-1})\|+\|\bm{\eta}_j\|\le Le_{j-1}+\delta$ with $e_0=0$, so
$e_k\le\delta\sum_{j=0}^{k-1}L^j$. A direct predictor computes $\hat{\Z}_k$ from observed inputs only, so its error at
horizon $k$ does not depend on its predictions at other horizons. $\square$

\subsection{Plug-in heads for published world models}\label{app:plugin}
For a published backbone with prediction $\mathbf{P}_k$, the head outputs $\hat\Z_k=(1-g_k)\mathbf{P}_k+g_k\mathbf{T}_k$,
where $\mathbf{T}_k$ is the local $7{\times}7$ soft transport (with identity bias) of \emph{observed} encoder tokens, and
the gate $g_k=\sigma(\mathbf{w}^\top\h+b)$ has $\mathbf{w}=\mathbf{0}$, $b=-4$ at initialisation, so training starts
numerically from the unmodified backbone. Nothing else in the backbone, loss or schedule changes.
\textbf{V-JEPA 2-AC.} Queries come from the predictor's normalised hidden state for each output block (1024-D), keys and
values from the layer-normalised ViT-g/16 tokens ($16{\times}16$, 1408-D) of the last $S{=}3$ measured frames, with a
learned per-source key offset (0.16M parameters). The fine-tuning objective is the released one (teacher-forced L1 plus
rollout L1). During rollouts the predictor consumes $\hat\Z_k$ as its own prediction, while $\mathbf{T}_k$ only ever
reads measured tokens.
\textbf{DINO-WM.} We subclass the official \texttt{ViTPredictor} (commit \texttt{0a9492f}, otherwise unmodified): queries
come from the predictor's output token, keys and values from its input token at the same slot, on the $14{\times}14$
DINOv2 patch grid ($S{=}1$); proprioceptive and action channels are left untouched. Keys and values are therefore
measured tokens only at the first rollout step: afterwards the input token at that slot is DINO-WM's own previous
prediction, so transport moves predicted features. We hypothesise, without proof, that this contributes to the Wall
regression. The extra parameters are drawn from a
private random generator, so data order, initialisation and dropout of the base predictor are identical in both arms.
Planning uses the official DINO-WM planner at a reduced budget that is identical for both arms.

\section{Extended experimental details}\label{app:exp}
\subsection{Benchmarks and evaluated tasks}\label{app:tasks}
\Cref{tab:tasks} states, for every benchmark, what the data show and what the world model is asked to do. In all
forecasting benchmarks the task is the same: given the last $H{=}3$ observed frames (encoded to frozen DINOv2 patch
grids), the actions between them and the next $K$ action blocks, predict the patch features of the next $K$ frames,
without seeing them. The benchmarks differ in embodiment, scene and action space. In the planning benchmarks the world
model is used inside a planner, and the task is to reach a goal image.
\begin{table}[h]
\centering\scriptsize\setlength{\tabcolsep}{3pt}
\caption{\textbf{Benchmarks and evaluated tasks.} Forecast: predict $K$ future feature grids from $H{=}3$ observed frames and $K$ future action blocks.}
\label{tab:tasks}
\begin{tabular}{@{}p{0.15\linewidth}p{0.33\linewidth}p{0.27\linewidth}p{0.2\linewidth}@{}}
\toprule
Benchmark & What is recorded & Evaluated task & Metric (test unit) \\
\midrule
DROID & Franka arm doing everyday manipulation in real homes, labs and offices; exterior camera & forecast $K{=}10$ steps (3.3\,s) given commanded end-effector poses and gripper & feature MSE, skill (held-out recording sessions) \\
DROID cam 2 & the same episodes seen from the second exterior camera & zero-shot transfer of the camera-1 model & as above \\
Open-H Hamlyn & dVRK arms on 7 surgical training tasks: knot tying, needle grasp and handover, peg transfer, suturing (2), tissue lifting, tissue retraction; scene camera & forecast $K{=}10$ steps (3.3\,s) given both arms' pose and gripper targets & feature MSE, skill (held-out episodes, per task) \\
BridgeData V2 & WidowX arm on tabletop and kitchen tasks (Open-X subset) & forecast $K{=}10$ steps (4\,s) given end-effector deltas and gripper & feature MSE, skill (held-out episodes) \\
RT-1 (Fractal) & mobile manipulator picking, placing, opening drawers in offices (Open-X subset; base-motion episodes removed) & forecast $K{=}10$ steps (3.3\,s) given end-effector deltas and gripper & feature MSE, skill (held-out episodes) \\
Language-Table & xArm pushing coloured, textured blocks on a table under language instructions; fixed camera, $640{\times}360$ (Open-X subset) & forecast $K{=}10$ steps (1\,s) given 2-D end-effector setpoint deltas & feature MSE, skill (held-out episodes) \\
IWS PushT / Box / Rope & real bimanual recordings of pushing a T-block, handling a box, and manipulating a rope (RLA-WM release) & forecast $K{=}12$ steps (2\,s) given the commanded joint targets & feature MSE on the 200 official validation handles per task \\
LeWM PushT / TwoRoom / Reacher & simulated pushing of a T-block, navigation through a doorway between two rooms, and a two-link arm reaching & plan actions with CEM so the predicted future matches a goal image & success rate over 50 episodes $\times$ 3 planner seeds \\
DINO-WM PushT / Wall & the DINO-WM releases of PushT and a point agent navigating through a door in a wall & latent prediction error; planning with the official DINO-WM planner & latent error; success rate \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Datasets and splits}\label{app:data}
\input{submission_folder/tables/datasets.tex}
\paragraph{DROID.} We use 24 official full-release shards (22.0\,GB) containing 1{,}126 episodes from 14 sites (327{,}498 native steps, 64{,}826 action blocks; 316/59/58 recording
sessions in train/val/test). Splits are assigned by recording session with a salted hash, so no session appears in two
splits. Frames are subsampled every 5 native steps (15\,Hz $\to$ 3\,Hz), and each action block
concatenates the 5 commanded Cartesian poses and gripper positions ($5\times7{=}35$).
Models are trained on the first exterior camera; the second exterior camera records the same episodes at the same
instants with the same action blocks from a different viewpoint; the main models never see it in training and are
evaluated on it zero-shot (\cref{fig:droid-cameras}).
\begin{figure}[h]
  \centering
  \includegraphics[width=\linewidth]{droid_cameras.pdf}
  \caption{\textbf{DROID camera 1 (training) and camera 2 (transfer)} on the held-out episode of \cref{fig:setting}. Right: true per-patch feature change from $t$ to $t{+}10$ on each camera's own $16{\times}16$ grid; the same arm motion changes different patches in each view.}
  \label{fig:droid-cameras}
\end{figure}
\paragraph{Open-H-Embodiment Hamlyn.} Seven dVRK training tasks, released under CC-BY-4.0 in LeRobot
format. We use the \texttt{observation.images.color} stream, which the release labels ``endoscope''
but which is an Intel RealSense scene camera ($848{\times}480$). One step is 1/3\,s: stride 10 at
30\,Hz, or stride 5 for the 15\,Hz tissue-lifting task. Each action block holds five commands at
15\,Hz, each an 8-D pose-and-gripper target for each arm ($5\times16{=}80$). Episodes shorter than 14
steps (6 of 1{,}019) are removed. Splits are 70/15/15 per task, assigned by a hash of the episode id.
\paragraph{IWS.} Real bimanual recordings from the RLA-WM release (PushT, Box, Rope; 640$\times$480 at 30\,fps). One
model step is 5 native command rows (1/6\,s), so the official 60-row horizon is $K{=}12$ steps; each action block
concatenates the 5 commanded joint-target rows. Train and validation use the upstream training recordings
(episode-disjoint, 15\% validation by a hash of the episode id); the test set is exactly the 200 official RLA-WM
validation handles per task, drawn from 10 reserved recordings.
\paragraph{BridgeData V2 and RT-1 (Fractal).} Pinned Open-X RLDS shards, primary camera, full frame resized to 224. For
Bridge (5\,Hz) one model step is 2 native steps (0.4\,s) and each block holds two 7-D actions (end-effector translation
and rotation deltas, gripper); for RT-1 (3\,Hz) one step is one native step (1/3\,s) with the same 7-D action, dropping
the base-motion dimensions and skipping the $\approx$1\% of episodes where the base moves.
\paragraph{Language-Table.} The real-robot Language-Table release \citep{lynch2023interactive} in Open-X RLDS form:
an xArm with a cylindrical end effector pushes rigid, coloured blocks on a wooden board, seen by a fixed camera
($640{\times}360$, resized to 224 like Bridge and RT-1 and shown at its native aspect). We fetch 14 of the 1{,}024 pinned
shards (6.0\,GB, 6{,}161 episodes). The Open-X sheet lists 10\,Hz control, so one model step is one native step (0.1\,s)
and the action is the 2-D delta end-effector setpoint. We do not subsample further: the episodes are short
hindsight-labelled segments of longer continuous play (median 12 native steps), so a 0.3\,s step would leave only 331 episodes long enough for
$H{+}K{=}13$ steps, and the end effector already moves $\approx$2.7\,cm per native step (median). Episodes shorter than 14
steps are dropped (3{,}572), leaving 2{,}589, split 10\%/9\%/81\% into test/val/train by a salted hash of the episode id. Open-X ships only a
\texttt{train} split, so every id keeps that name (\texttt{train00160\_052}: shard 160, record 52) and we carve
validation and test from it. The split is by segment: the release has no id of the source play episode, so segments
of one play session can fall into different splits and share a scene layout. In our subset we find no direct
continuations: for only 1 of 264 test segments does a training segment start or end with a frame closer to the test
segment's boundary frame than the median one-step frame change,
but source-level disjointness is not guaranteed.
\paragraph{Planning suites.} PushT, TwoRoom and Reacher expert datasets from the LeWM release
(HDF5). Episodes are split disjointly into train and validation sets for world-model training.
Evaluation follows the official protocol (\cref{app:planning}).

\subsection{Baselines}\label{app:baselines}
\paragraph{Matched predictors.} All learned baselines share \ours{}'s memory encoder, decoder, width,
depth, optimiser and schedule, and differ only in the output head and rollout. This isolates the
contribution of transport from those of capacity and spatial communication. Direct has the same
cross-patch attention as \ours{} and differs only in lacking the transport and gate.
\paragraph{DINO-WM-style (AR-TF).} Following \citet{zhou2025dinowm}, the predictor regresses absolute
next-step patch features with teacher forcing and is rolled out autoregressively at test time. We
use the same frozen DINOv2 patches at native resolution.
\paragraph{External reference: V-JEPA 2-AC.} We evaluate the released V-JEPA 2-AC \citep{assran2025vjepa2}
(ViT-g encoder and 300M action-conditioned predictor, trained on DROID) zero-shot on exactly our test windows, with its own
preprocessing, action convention and autoregressive rollout. Because feature spaces differ, we compare the fraction of
persistence error each model removes in its own space (\cref{tab:external}); we also fine-tune it on our training split with
and without the \ours{} head under an identical budget. Its training data likely include our test trajectories.
\input{submission_folder/tables/external.tex}

\subsection{Training and compute}\label{app:training}
\paragraph{Training.} AdamW ($\beta{=}(0.9,0.95)$, weight decay 0.05), peak learning rate
$3{\times}10^{-4}$ with 800 warm-up steps and cosine decay, 16k steps, batch 48, bf16 autocast,
gradient clipping at 1.0. Validation MSE (averaged over horizons, windows at stride 2) is computed
every 2k steps, and the best checkpoint is used for testing. AR is trained through its full
10-step rollout. AR-TF is trained with teacher forcing and rolled out at test time. All features for
a split are kept in GPU memory as float16 and windows are gathered by index.

\input{submission_folder/tables/hyperparams.tex}

\paragraph{Compute.} All experiments ran on NVIDIA H200 GPUs, packing at most two learned predictors
per GPU. \pend{}

\subsection{Planning protocol}\label{app:planning}
Closed-loop planning follows the official LeWM and stable-worldmodel evaluation (\cref{tab:plan-protocol}). Every
predictor, including the released LeWM checkpoint, is wrapped in the same pluggable interface and driven by the same CEM
planner, goal sampling, budget and success criteria; only the world model changes. The planning cost is the squared
distance between the final predicted latent and the goal latent. \Cref{app:planning-results} checks that the harness
reproduces the published LeWM results.
\input{submission_folder/tables/planning_protocol.tex}
\subsection{Statistical procedures}\label{app:stats}
Each test episode contributes the mean of its windows (stride 2). For each method and seed we
average over episodes, and tables report the mean over three training seeds. For a paired
comparison between methods A and B, we compute per-episode differences (averaged over seeds) and
draw 10{,}000 bootstrap resamples of episodes to form a percentile 95\% interval. For DROID, we
resample sessions instead of episodes to respect the session structure. Primary contrasts (\ours{}
against each baseline on each dataset) are Holm-adjusted within each dataset.
\section{Extended results}\label{app:results}
\subsection{Full forecasting results}\label{app:full}
\input{submission_folder/tables/full_per_horizon.tex}
\input{submission_folder/tables/hamlyn_per_task.tex}
\input{submission_folder/tables/iws_per_task.tex}
\pend{}

\subsection{Additional ablations}\label{app:ablations}
\input{submission_folder/tables/recipes.tex}
\paragraph{Training recipes.} Because halving training to 8k steps barely changes test error (\cref{tab:recipes}), we
tested whether stronger regularisation or more data helps (\cref{tab:recipes}): training on
both exterior cameras with EMA weights, dropout and memory-token dropout, and adding explicit correlation (cost-volume)
features between consecutive observed grids to every method. \pend{}
\begin{figure}[h]
  \centering
  \figorpending{ablation_window.pdf}{3.4cm}{Validation MSE vs.\ transport window size $w\in\{3,5,7,11,\text{global}\}$
  and number of source frames $S\in\{1,3\}$.}
  \caption{\textbf{Transport window and memory size.}}
  \label{fig:ablation-window}
\end{figure}
\pend{}

\subsection{Plug-in training dynamics}\label{app:plugin-dynamics}
This subsection supports the plug-in contribution (\cref{sec:plugin}).
\begin{figure}[h]
  \centering
  \includegraphics[width=\linewidth]{vjepa_plugin.pdf}
  \caption{\textbf{Plug-in heads across backbones}, each backbone trained with (green) and without (blue) the
  \ours{} head under an identical budget. (a--c) Validation latent error during training; V-JEPA 2-AC: 2 seeds (band =
  range), DINO-WM: 1 seed. (d) Mean gate of the V-JEPA head (DINO-WM does not log it). (e--g) Change in latent error
  from adding the head: V-JEPA per horizon on the test split (bars: seed mean, lines: range over seed pairings);
  DINO-WM per open-loop rollout step and teacher-forced (TF) on the validation split. Green: lower error with the head;
  red: higher. (h) DINO-WM planning success (official MPC-CEM planner, reduced budget, planner seed 99); the other
  cells had not finished when this version was compiled.}
  \label{fig:vjepa-plugin}
\end{figure}
\Cref{fig:vjepa-plugin} shows every plug-in run we trained.
\textbf{V-JEPA 2-AC.} Both fine-tuning arms run for their full 3{,}000-step budget.
With the head, validation error is lower at every evaluation step for both seeds. The gate starts at $0.018$, so the
model begins as the released predictor, and opens to about $0.55$ within the first 250 steps, rising to about $0.65$: the
backbone learns to route most of its forecast through transport of measured tokens. Both arms reach their best
validation error early and then slowly overfit our training split; the test numbers use the best validation checkpoint
of each run. The gain holds at every horizon: test error is 15\% lower at $k{=}1$ and 17\% lower at $k{=}10$, and the
skill gain grows from $+11.5$ points at $k{=}1$ to $+13.1$ at $k{=}10$.
\textbf{DINO-WM PushT} (3 epochs, the full budget of both arms). The head lowers teacher-forced latent error by 8\% and
open-loop error by 6\% at the first rollout step and 11\% at the fifth; its validation curve starts slightly higher and
drops below the baseline only in the last epoch, so this short schedule is a limitation.
\textbf{DINO-WM Wall} (15 epochs, full budget). Here the head hurts: teacher-forced error is 18\% higher and open-loop
error 23--39\% higher. With the head the validation error is far lower after the first epoch (0.15 against 1.42),
but the baseline overtakes it at epoch 5; the head's curve is still falling at the end of the budget while the
baseline's has flattened, and whether a longer schedule closes the gap is untested.
Planning success is available only for the PushT baseline (62\%); the runs with the head on PushT and both Wall runs
are queued, so the effect of the head on DINO-WM planning is not yet known.

\subsection{Decoded-pixel metrics}\label{app:pixel}
\input{submission_folder/tables/pixel_metrics.tex}
For each dataset we train one feature-to-RGB decoder on true training features (L1 + LPIPS) and apply it
unchanged to every method's forecasts, so differences reflect the predicted features only.
\pend{}

\subsection{Probing predicted features}\label{app:probes}
\input{submission_folder/tables/probes.tex}
Ridge probes map mean- and $4{\times}4$-pooled features to the commanded DROID end-effector position or the
measured Hamlyn instrument-tip positions (both arms pooled). They are fit on true training features, with
the ridge strength chosen on validation, and are evaluated on true versus predicted test features at $k{=}10$.
\pend{}

\subsection{Planning results and reproduction check of the harness}\label{app:planning-results}
\paragraph{Forecast error bounds planning regret.} Let the planner score an action sequence $\mathbf{a}$ by
$\hat c(\mathbf{a})=\|\hat{\mathbf{z}}_T(\mathbf{a})-\mathbf{z}_g\|^2$, where $\mathbf{z}_g$ is the goal encoded by the same
frozen encoder, and let $c(\mathbf{a})$ use the true future $\mathbf{z}_T(\mathbf{a})$. If
$\|\hat{\mathbf{z}}_T-\mathbf{z}_T\|\le\varepsilon$ and $\|\mathbf{z}_T-\mathbf{z}_g\|\le R$, then
$|\hat c-c|=|2\langle\hat{\mathbf{z}}_T-\mathbf{z}_T,\mathbf{z}_T-\mathbf{z}_g\rangle+\|\hat{\mathbf{z}}_T-\mathbf{z}_T\|^2|\le2R\varepsilon+\varepsilon^2$.
A plan $\hat{\mathbf{a}}$ within $\eta$ of the best predicted cost therefore satisfies
$c(\hat{\mathbf{a}})\le\min_{\mathbf{a}}c(\mathbf{a})+4R\varepsilon+2\varepsilon^2+\eta$: lower forecast error tightens the
regret bound, but only for errors along directions that change the ranking of candidate plans, which is why we also
measure action sensitivity (\cref{app:efficiency}).

\paragraph{Reproduction check.} With the released LeWM PushT checkpoint and the official protocol,
our harness reaches 45/50, 44/50 and 43/50 successes for planner seeds 42/43/44 (mean 88\%). An
adapter check confirms that our pluggable predictor interface produces exactly the native LeWM
planning costs (maximum absolute difference 0). The LeWM paper reports $96\pm2.8$, averaged over
training seeds, whereas the released checkpoint is a single training seed. On TwoRoom and Reacher the
released checkpoints reach 43/41/44 and 42/44/41 successes (85.3\% and 84.7\%), against 87 and 86
reported by \citet{maes2026lewm}. Planning takes 0.41\,s (PushT) and about 1.5\,s (TwoRoom, Reacher)
per CEM plan on one H200. As an independent check, LeWM's own evaluation code (an earlier release of the planning library) gives 46/50 on PushT and 39/50 on Reacher for the same checkpoint and seed 42, against 45/50 and 42/50 in our harness; the Reacher difference of 3 episodes is within planner-seed variation (41--44 in our runs) but we report it for transparency.

\paragraph{Random-action floor.} Uniform random actions from each environment's action space, on the same tasks, seeds
and 50-step budget, succeed in 0.7\% (PushT), 28.7\% (TwoRoom) and 15.3\% (Reacher) of episodes (\cref{tab:planning}).
TwoRoom and Reacher thus have a sizeable floor: some goals lie within reach of undirected motion.

\paragraph{Steps to success and terminal error.} \Cref{tab:planning-extra} reports two further metrics over the same
episodes: the number of environment steps until the first success, with failures counted at the budget, and the
physical distance to the goal after the last executed step (an episode stops at its first success), in the units of the
success test. Both are recorded by the harness; the LeWM errors come from a re-run with the same checkpoint, tasks and
planner seeds that reproduces every per-episode outcome of \cref{tab:planning}.
\input{submission_folder/tables/planning_extra.tex}

\subsection{Action sensitivity and efficiency}\label{app:efficiency}
\begin{figure}[h]
  \centering
  \includegraphics[width=\linewidth]{tradeoff.pdf}
  \caption{\textbf{Forecast skill versus action ranking, per benchmark (test).} (a--c) $x$: skill (\% of persistence
  error removed, mean over $k$); $y$: action-ranking error, the share of windows in which another episode's actions give
  a lower error than the true ones (log scale; lower right is better). Large markers: seed mean; crosshairs: 95\%
  bootstrap CIs (sessions for DROID, episodes elsewhere); faint markers: single seeds; hollow circles: DROID ablations
  (the four \ours{} variants lie on top of \ours{}). Hamlyn and Language-Table have one seed; Language-Table has no AR-TF
  run. (d) Error reduction of \ours{} over Direct in quintiles of per-episode motion (persistence error), 95\% CIs.}
  \label{fig:tradeoff}
\end{figure}

\paragraph{Accuracy versus action sensitivity.} A forecast that stays close to $\Z_0$ can score
well on average while barely responding to the commanded actions, and a planner then cannot rank
candidate action sequences. We therefore measure the action-ranking error: the share of test windows in which actions
from another episode yield a lower forecast error than the true actions (\cref{fig:tradeoff}). We use it rather than
the raw change of the forecast under swapped actions, which also rewards forecasts that move in the wrong direction: on
DROID, AR-TF changes its forecast most under swapped actions yet ranks worse than Direct. On DROID, \ours{} has both the
highest skill (\toDroidSkillShift\%) and the lowest ranking error (\toDroidErrShift\%, against \toDroidErrDirect\% for
Direct, \toDroidErrAR\% for AR and \toDroidErrARTF\% for AR-TF). The four \ours{} ablations (correlation features,
dilated window, two transport iterations, both) stay within the seed spread of \ours{} on both axes, whereas a residual
AR-TF gains skill but ranks worse. On Hamlyn, \ours{} is again best on both axes, although its ranking error
(\toHamErrShift\%) overlaps with Direct (\toHamErrDirect\%) and AR (\toHamErrAR\%). On Language-Table, \ours{} has the
highest skill but not the fewest ranking errors: Direct errs on \toLTErrDirect\% of windows and \ours{} on
\toLTErrShift\% (overlapping CIs, one seed), so once ranking errors are this rare a lower forecast error does not
translate into better ranking. The gain over Direct is positive in every motion quintile on all three benchmarks
(\cref{fig:tradeoff}d). On DROID it is largest for the stillest episodes (\toDroidGainStill\%) and smallest for the
fastest (\toDroidGainFast\%); per patch, it peaks at intermediate motion and grows with the horizon
(\cref{fig:anatomy}). No
planning run of these predictors exists yet, so the figure does not encode planning success.

\input{submission_folder/tables/efficiency.tex}
\paragraph{Efficiency.} \Cref{tab:efficiency} compares parameters, compute per forecast step and
latency. Transport adds \pend{} parameters. Because \ours{} and Direct decode all $K$ steps in
parallel, a $K$-step forecast needs one encoder pass, whereas recursive predictors need $K$
(\pend{}).


\section{Extended analysis and discussion}\label{app:analysis-all}
\subsection{Analysis details}\label{app:analysis}
Each analysis below states the contribution it supports: the method (transport carries moving content and the identity
path keeps static content), the motivating motion model (Propositions~\ref{prop:bayes} and~\ref{prop:residual}), or the
evaluation of where the gains arise.
\input{submission_folder/tables/regions.tex}
\begin{figure}[h]
  \centering
  \figorpending{mechanism.pdf}{5.2cm}{How \ours{} works: forecasts in feature space, the additive decomposition, and sharpness.}
  \caption{\textbf{Forecasts in feature space.} (a) Held-out forecasts at $k{=}10$ (shared PCA to colour). (b) Spatial contrast kept by each forecast, relative to the truth (95\% CIs over test episodes).}
  \label{fig:mechanism}
\end{figure}
\begin{figure}[h]
  \centering
  \includegraphics[width=\linewidth]{geometry.pdf}
  \caption{\textbf{Feature-space geometry on held-out DROID ($k{=}10$).} (a) Three moving patches (fixed selection rule) in a 2-D PCA of their observed candidates. (b) Moving-patch error, with the model-free oracle move. (c) Per-patch error difference to Direct.}
  \label{fig:geometry}
\end{figure}
\paragraph{Feature-space geometry (supports Proposition~\ref{prop:bayes}).} \Cref{fig:geometry} is the data counterpart of \cref{fig:featurespace}. Panel (a)
shows, for one patch at a time, the $S w^2{=}147$ observed candidate features of its transport window projected to 2-D
by PCA, sized by the learned transport weight, together with the observed feature at the same location, the true future
and the three forecasts. The patches illustrate the mechanism where it matters most: among moving, high-gate ($g{>}0.5$)
patches with above-median change, the three (from different episodes) with the largest advantage of \ours{} over the
better baseline. They are selected, not typical; the typical behaviour is given by the aggregates. Panels (b) and (c) aggregate over all moving patches
of \geoWindows{} windows from \geoEpisodes{} test episodes. The oracle move picks, for each patch, the single observed
candidate closest to the true future; it is a model-free reference, not an achievable predictor, since it uses the
future to choose. Gain recovered is $(\text{err}_{\text{stay}}-\text{err}_{\text{model}})/(\text{err}_{\text{stay}}-\text{err}_{\text{oracle}})$.
Paired episode-bootstrap 95\% intervals of the moving-patch error difference at $k{=}10$ exclude zero against both
Direct and AR.

\begin{figure}[h]
  \centering
  \includegraphics[width=\linewidth]{anatomy.pdf}
  \caption{\textbf{Anatomy of the gains (held-out DROID).} (a,b) Error reduction over horizon and motion decile. (c) Gate on moving (area) and static (dashed) patches. (d) Share of the oracle-move gain recovered on moving patches.}
  \label{fig:anatomy}
\end{figure}
\paragraph{Anatomy of the gains (supports the method).} \Cref{fig:anatomy} uses all \geoWindows{} test windows. In (a,b) every patch at every
horizon is assigned to a decile of its true change $\|\mathbf{z}_{k,i}-\mathbf{z}_{0,i}\|^2$ (deciles pooled over horizons), and each
cell reports $1-\text{err}_{\ours{}}/\text{err}_{\text{baseline}}$; the $10{\times}10$ grid is drawn as filled contours after cubic interpolation, with the maximum marked. Against Direct the gain is largest for moderate-to-large
motion at long horizons; against AR it is largest on nearly static patches, where the recursive rollout accumulates
error on content that should not change. (c) The forecast departs from $\Z_0$ as $g(\mathbf{T}-\Z_0)+\mathbf{r}$ exactly;
on moving patches the mean gate rises from \anaGateOne{} at $k{=}1$ to \anaGateTen{} at $k{=}10$, and the moved term carries
\anaMoveShare\% of the squared departure on average, so the correction remains a small residual. (d) On moving patches \ours{} recovers a larger share of the oracle-move gain than both baselines at every horizon, reaching \geoShareShift\% at $k{=}10$ against \geoShareDirect\% (Direct) and \geoShareAR\% (AR).

\begin{figure}[h]
  \centering
  \figorpending{flow_agreement.pdf}{3.6cm}{Patch-level agreement between the transport field and RAFT
  optical flow (cosine of displacement, end-point error) vs.\ horizon; distribution over test episodes.}
  \caption{\textbf{Transport vs.\ optical flow.}}
  \label{fig:flow}
\end{figure}
\paragraph{Transport versus optical flow.} \Cref{fig:flow} compares the expected source displacement of each patch with
RAFT optical flow \citep{teed2020raft} pooled to the patch grid. \pend{}

\begin{figure}[h]
  \centering
  \includegraphics[width=\linewidth]{trails.pdf}
  \caption{\textbf{Transport trails vs.\ optical flow (held-out DROID).} (a) Expected source location of moving patches (solid) and RAFT backward-flow trail (dashed), $k{=}1,\dots,10$, in windows at the 25/50/75th percentile of trail error. (b) Trail endpoint error on all moving patches (95\% CIs).}
  \label{fig:trails}
\end{figure}
\paragraph{Transport trails.} For each query patch $i$ and horizon $k$, the trail point is $i+\mathbf{o}_k(i)$, with
$\mathbf{o}_k(i)$ the $\pi$-weighted mean offset over the window and the three source frames; the true trail is
$i+\mathbf{b}_k(i)$, with $\mathbf{b}_k$ the RAFT flow from frame $t_0{+}k$ back to $t_0$ pooled to the grid (the
protocol of \cref{fig:flow}; no pooled flow was cached, so it is recomputed). Moving patches have $|\mathbf{b}_{10}|>0.5$
patch (\trPatches{} patches, \trWindows{} windows, \trEpisodes{} episodes). The trails point the right way (cosine
\trCosTen{} at $k{=}10$) but are much shorter than the true motion: the endpoint error (\trEpeTen{} patches at $k{=}10$)
is above the zero-motion baseline up to $k{=}8$ and only slightly below it at $k{=}10$ (\trZeroTen{}), and the
$7{\times}7$ window explains little of the gap (reach limit \trReachTen{}). The transport is therefore not an optical-flow
estimator: it fetches matching features mostly from nearby positions and earlier frames.

\begin{figure}[h]
  \centering
  \includegraphics[width=\linewidth]{counterfactual.pdf}
  \caption{\textbf{Counterfactual actions (held-out DROID).} (a) One history under the true and three re-anchored real command sequences: commanded path (top view, dots $k{=}1,\dots,10$), transport motion $-g\,\mathbf{o}$, and the forecast. (b,c) All test windows: forecast change and error increase when the plan is swapped.}
  \label{fig:counterfactual}
\end{figure}
\paragraph{Counterfactual actions (supports action-conditioned transport).} DROID commands are absolute end-effector
poses, so every alternative plan is a recorded test sequence re-anchored at the history's last command (only its start
moves). In (a) the history is the teaser window and the alternatives come from other episodes by a fixed rule: the
direction opposite to the true plan, upward motion, and the shortest commanded path, all with a constant gripper. In
(b,c) each of the \cfWindows{} windows (\cfEpisodes{} episodes) receives the re-anchored plan of the window half the test
set away. \ours{} reacts most to the plan on moving patches (change \cfChgMovS{} vs.\ \cfChgMovD{} for Direct and
\cfChgMovA{} for AR at $k{=}10$) and gains most from the true plan (error increase \cfIncS{} vs.\ \cfIncD{} and \cfIncA{}).
It is not more selective, however: the moving-to-static change ratio is similar for all three (\cfSelS{}, \cfSelD{},
\cfSelA{}), and under a wrong plan its moving-patch error (\cfErrAltS{}) exceeds that of AR (\cfErrAltA{}).

\begin{figure}[h]
  \centering
  \includegraphics[width=\linewidth]{winmap.pdf}
  \caption{\textbf{Per-episode win map (held-out DROID).} (a) Episode motion. (b,c) Relative error difference of \ours{} to Direct and AR per episode and horizon (green: \ours{} better; $\triangleleft$: \ours{} loses on average over $k$). (d) Horizon-averaged difference vs.\ motion.}
  \label{fig:winmap}
\end{figure}
\paragraph{Where \ours{} wins and loses.} \Cref{fig:winmap} uses the \cref{tab:main} test evaluation of all \wmEpisodes{}
episodes (seed 0, the checkpoints of all analyses), sorted by their mean true feature change. Averaged over horizons,
\ours{} beats Direct on \wmWinDirect\% and AR on \wmWinAR\% of the episodes. It loses on \wmLoseDirect{} and \wmLoseAR{}
episodes (\wmCellsDirect\% and \wmCellsAR\% of the episode--horizon cells), all in the less-moving half, and none of these
losses occurs in all three seeds. The relative gain shrinks with episode motion (Spearman $\rho=\wmRhoDirect$ vs.\ Direct,
\wmRhoAR{} vs.\ AR; median gain over Direct \wmMedQOneDirect\% in the lowest and \wmMedQFourDirect\% in the highest motion
quartile); \cref{fig:anatomy} gives the patch-level view within episodes.

\subsection{Segmentation view of the forecasts}\label{app:segments}
\begin{figure}[h]
  \centering
  \figorpending{segments.pdf}{5.4cm}{Segmentation read out of the forecasts: SAM 2.1 masks, forecast masks, IoU vs horizon.}
  \caption[Segmentation view of the forecasts]{\ifdefined\segCaption\segCaption\else Segmentation view of the forecasts.\fi}
  \label{fig:segments}
\end{figure}
\IfFileExists{submission_folder/tables/generated/segments_decoded_text.tex}{\input{submission_folder/tables/generated/segments_decoded_text.tex}}{}

\paragraph{Does the moving arm land in the right place? (Evaluation beyond feature error.)} Feature error does not show \emph{where} a forecast puts the
arm, so we read a segmentation out of every forecast (\cref{fig:segments}). SAM~2.1 \citep{ravi2025sam2} tracks the robot
arm (the instruments on Hamlyn) through the true future frames, and each forecast patch is labelled arm or background by
nearest-neighbour transfer from the observed frame alone (protocol below). \emph{Placement} is the distance between the
centroid of the cells a forecast labels as arm and the centroid of the true mask at $t{+}k$. On the half of the
\segDroidWindows{} held-out DROID windows where the arm moves most, the mean placement error over $k$ is
\segDroidPlaceSW{}\,px for \ours{}, \segDroidPlaceDi{} for Direct, \segDroidPlaceAR{} for AR and \segDroidPlacePers{} for
persistence (\segDroidPlaceFrame{} frame): \ours{} removes \good{\segDroidPlaceRedAR{}\,px} relative to AR (95\% CI
\segDroidPlaceRedARCI{}), but only \segDroidPlaceRedDi{}\,px relative to Direct (\segDroidPlaceRedDiCI{}), a marginal
difference; at $k{=}10$ \ours{} and Direct tie (\segDroidPlaceSWLast{} vs.\ \segDroidPlaceDiLast{}\,px). On Hamlyn all
learned models place the instruments within a pixel of each other (\ours{} \segHamlynPlaceSW{}, Direct
\segHamlynPlaceDi{}, AR \segHamlynPlaceAR{}\,px; paired reductions \segHamlynPlaceRedDi{} \segHamlynPlaceRedDiCI{} and
\segHamlynPlaceRedAR{} \segHamlynPlaceRedARCI{}\,px), all far ahead of persistence (\segHamlynPlacePers{}\,px). Overlap
tells the same story: on moving DROID windows the arm read out of \ours{} has IoU \good{\segDroidMovGainDi{}} points above
Direct (\segDroidMovGainDiCI{}) and \good{\segDroidMovGainAR{}} above AR (\segDroidMovGainARCI{}); on Hamlyn it ties
Direct (\segHamlynMovGainDi{}, \segHamlynMovGainDiCI{}; \cref{tab:segments}). The examples in \cref{fig:segments}a are
selected for a large \ours{} placement advantage and illustrate what that advantage looks like; the claims rest on the
averages.

\paragraph{Protocol.} We ask whether the moving part of the scene lands where it should in each forecast, without
decoding pixels. \emph{Reference masks.} On the observed frame $t$ (the last history frame) of every held-out window
(test split; the planning suites have no test split, so their validation split is used; stride 2 over all episodes, and an evenly spaced subset of 1{,}500 windows where there are more),
Grounding DINO \citep{liu2024groundingdino} detects a fixed per-dataset text prompt (DROID, Bridge, RT-1: ``a robot arm'';
Hamlyn: ``a surgical instrument'', up to two non-overlapping boxes; Language-Table: ``a robot arm. a block.'', up to two boxes; IWS: the manipulated T block, box or rope). Its
top box prompts SAM~2.1 (Hiera-L) \citep{ravi2025sam2}, which segments frame $t$ and \emph{tracks} the mask through the
true future frames $t{+}1,\dots,t{+}K$ with its video memory. Frames are segmented at their native aspect ratio; each
mask keeps its largest connected component and any component with $\ge$25\% of its area. For the rendered planning
suites a text prompt is unreliable, so an exact colour threshold of the agent/object is used instead. \emph{Quality
control.} A window is kept only if the detection score is $\ge 0.3$, the mask covers 3--128 of the 256 patches at $t$,
the track is never lost and the mask area never changes by more than $3\times$ between consecutive frames; on DROID
this keeps \segDroidWindows{} of \segDroidConsidered{} windows (rejected: \segDroidRejNoDetection{} without a
confident detection, \segDroidRejFgAreaAtT{} with an arm too small or too large at $t$, \segDroidRejLostTrack{} lost
tracks, \segDroidRejAreaJump{} area jumps), and on Hamlyn \segHamlynWindows{} of \segHamlynConsidered{}.
\emph{Reading a mask out of a forecast.} Masks are area-averaged onto the $16{\times}16$ patch grid (a patch is
foreground if $\ge$50\% covered). Each patch of a forecast $\hat\Z_k$ is labelled by nearest-neighbour transfer
from the observed frame only: it is foreground if its most similar (cosine) foreground patch of $\Z_0$ is more similar
than its most similar background patch. This labeller was fixed in a DROID pilot as the one with the highest IoU when
applied to the \emph{true} future features, a criterion that involves no forecast (on DROID at $k{=}10$:
nearest-neighbour \segDroidOracleNn{}, $k$-means prototypes \segDroidOracleMulti{}, single prototypes
\segDroidOracleProto{}). \emph{Metric.} IoU between the forecast mask and the tracked SAM mask of the true future frame,
per horizon and averaged over $k$, on all windows and on \emph{moving} windows (overlap of the masks at $t$ and $t{+}K$
below the median). CIs are 95\% bootstrap intervals over episodes; gains are paired differences per episode.
\emph{Placement.} The predicted centroid is the mean of the centres of the cells labelled arm; the true centroid is
the coverage-weighted mean of cell centres under the tracked SAM mask; distances are in pixels of the displayed
native-aspect frame (DROID \segDroidPlaceFrame{}, Hamlyn \segHamlynPlaceFrame{}). At a given $k$, windows in which any compared method labels no cell are dropped for all methods (paired; this
affects at most 1\% of DROID and 3\% of Hamlyn windows). \emph{Examples} show where \ours{} does best, by a fixed rule: among QC-passing moving
windows in which \ours{}'s $k{=}10$ placement error is below its median and the labeller applied to the \emph{true} future
features is at least median-accurate (a method-independent sanity check), the two (from distinct episodes) with the
largest $k{=}10$ placement advantage $\min(\text{Direct},\text{AR})-\text{\ours{}}$. All methods are always shown on the
same window and the same frame. The examples are not representative; averages and paired CIs are in
\cref{tab:placement,tab:segments}. Checkpoints are the final ones used in the main tables.
\paragraph{Results.} \Cref{tab:placement} lists placement error and \cref{tab:segments} IoU per dataset and method (\cref{fig:segments-droid,fig:segments-openh-hamlyn}
show both example windows per dataset). On DROID, \ours{} has the highest IoU at every reported horizon, on all and on
moving windows, with paired CIs that exclude zero against both Direct and AR. On Hamlyn the instruments move less
within ten steps and all three learned predictors are within half an IoU point: \ours{} ties Direct and is marginally
ahead of AR; the segmentation view does not separate the methods there. Datasets without results are marked pending.
\paragraph{Caveats.} (i) The readout measures where a forecast puts content that \emph{looks like} the observed
foreground; it cannot credit content that was not visible at $t$. (ii) The labeller is imperfect: applied to the true
future features it reaches IoU \segDroidOracleNn{} on DROID at $k{=}10$, so absolute values are lower bounds on
localisation quality; on Hamlyn several forecasts slightly exceed this reference, because smoother forecasts can be
labelled more easily than the true features. (iii) SAM~2.1 occasionally includes a grasped object or tool-attached
structure in the tracked mask; QC removes lost or exploding tracks but
not such merges. On Hamlyn most rejected windows (\segHamlynRejFgAreaAtT{} of \segHamlynRejected{}) are close-ups in which
the instruments cover more than half of the frame. (iv) One seed per method.

\begin{table}[h]
  \centering\scriptsize
  \caption{\textbf{Segmentation view: IoU of the foreground read out of each forecast} (horizon-averaged, $k{=}5$ and
  $k{=}K$; all and moving windows). \good{Green}: paired 95\% CI of \ours{} minus \emph{both} Direct and AR excludes
  zero.}
  \label{tab:segments}
  \begin{tabular}{llccccccc}
    \toprule
    & & \multicolumn{3}{c}{all windows} & \multicolumn{2}{c}{moving windows} & \multicolumn{2}{c}{\ours{} gain, moving (IoU pts)} \\
    \cmidrule(lr){3-5}\cmidrule(lr){6-7}\cmidrule(lr){8-9}
    Dataset & Method & avg.\ over $k$ & $k{=}5$ & $k{=}K$ & avg.\ over $k$ & $k{=}K$ & vs Direct & vs AR \\
    \midrule
    \inputrows{submission_folder/tables/generated/segments_rows.tex}
    \bottomrule
  \end{tabular}
\end{table}
\begin{table}[h]
  \centering\scriptsize
  \caption{\textbf{Arm placement error} (px of the displayed frame, DROID $320{\times}180$, Hamlyn $396{\times}224$; lower is better). Gains: moving windows, mean over $k$, paired 95\% CI; \good{green}: CI excludes zero.}
  \label{tab:placement}
  \begin{tabular}{llccccccc}
    \toprule
    & & \multicolumn{2}{c}{all windows} & \multicolumn{3}{c}{moving windows} & \multicolumn{2}{c}{error removed by \ours{} (px)} \\
    \cmidrule(lr){3-4}\cmidrule(lr){5-7}\cmidrule(lr){8-9}
    Dataset & Method & avg.\ over $k$ & $k{=}K$ & avg.\ over $k$ & $k{=}5$ & $k{=}K$ & vs Direct & vs AR \\
    \midrule
    \inputrows{submission_folder/tables/generated/segments_place_rows.tex}
    \bottomrule
  \end{tabular}
\end{table}
\IfFileExists{submission_folder/tables/generated/segments_figs.tex}{\input{submission_folder/tables/generated/segments_figs.tex}}{}

\subsection{Qualitative results}\label{app:qualitative}
\paragraph{Example selection.} Illustrations in the main paper use selected held-out windows, and every method is always shown
on the same window. The teaser strip, the bottom row of \cref{fig:method} and \cref{fig:mechanism} use the DROID test
window (among the 30\% with the most motion) with the largest step-10 advantage of \ours{} over the better of Direct and
AR; the win maps of \cref{fig:qualitative} use the high-motion windows with the largest advantage over Direct. These show
the mechanism where it matters; typical behaviour is given by the aggregate results, and the galleries below use a random
selection. In \cref{fig:teaser}(b) every model's own step-10 features (and the true ones) are decoded to RGB by the
feature-to-RGB decoder of \cref{app:pixel}, shown at the camera's aspect ratio and cropped to the same arm region (top-left
$11{\times}11$ patches); the corner numbers are feature errors of the whole frame. In \cref{fig:teaser}(c), $\Delta$ error
compares \ours{} with Direct (the strongest learned baseline) on DROID and Hamlyn, and with the same published model without
the head for V-JEPA~2-AC and DINO-WM; skill is the share of persistence error removed.

\paragraph{Largest-advantage windows per benchmark.} For every forecasting benchmark with finished \ours{}, Direct and AR
runs (lowest common seed), the next figures show three held-out windows chosen by a fixed rule: over all test windows
(stride 2) we compute each method's step-10 feature MSE and the true change $\|\Z_{t+10}-\Z_t\|^2$; among windows in the
top 50\% of true change we rank by the gain $1-\mathrm{err}_{\text{\ours{}}}/\min(\mathrm{err}_{\text{Direct}},\mathrm{err}_{\text{AR}})$
and keep the top three from distinct episodes. Every method is shown on the same window and frame, with one error scale
per dataset; decoded forecasts use the decoder of \cref{app:pixel}. These are selected best-case examples, not typical
ones: the random galleries follow, and aggregate results are in the tables.
\IfFileExists{submission_folder/tables/generated/qual_best_figs.tex}{\input{submission_folder/tables/generated/qual_best_figs.tex}}{}

\begin{figure}[h]
  \centering
  \figorpending{gallery_droid.pdf}{7cm}{Extended DROID gallery: 6 random test episodes; observed frame,
  ground truth and decoded forecasts from AR, Direct and \ours{} at $k\in\{1,3,5,10\}$, transport arrows and gates.}
  \caption{\textbf{DROID test rollouts (random selection).}}
  \label{fig:gallery-droid}
\end{figure}
\begin{figure}[h]
  \centering
  \figorpending{gallery_surgical.pdf}{7cm}{Extended Open-H Hamlyn gallery: one random test episode per task (7 tasks).}
  \caption{\textbf{Surgical test rollouts (one per task, random selection).}}
  \label{fig:gallery-surgical}
\end{figure}
\begin{figure}[h]
  \centering
  \figorpending{gallery_language_table.pdf}{7cm}{Language-Table gallery: 6 random test episodes; observed frame,
  true and decoded frames at $k\in\{1,3,5,10\}$ for AR, Direct and \ours{}, gate and transport.}
  \caption{\textbf{Language-Table test rollouts (random selection).} Frames at native $16{:}9$ aspect.}
  \label{fig:gallery-langtable}
\end{figure}
\begin{figure}[h]
  \centering
  \figorpending{failures.pdf}{4.5cm}{Largest-error test windows for \ours{}: disocclusion, fast motion,
  contact events; with transport fields and gates.}
  \caption{\textbf{Failure cases.}}
  \label{fig:failures}
\end{figure}


\subsection{Discussion}\label{app:discussion}
\paragraph{When transport helps.} The gains are largest where content moves within the transport window and keeps its
appearance. On DROID, \ours{} has \droidMovingVsDirect\% lower moving-patch error than Direct, and its advantage holds in
every decile of motion (+\decileMin\% to +\decileMax\%). On the surgical tasks the instruments move less within ten
steps, the gains are small (\hamlynVsDirect\% vs.\ Direct), and the segmentation view does not separate the learned
models (\cref{app:segments}).
\paragraph{When it does not.} As a plug-in to DINO-WM on Wall, where the backbone conditions on a single history frame,
the head raises latent error by \dinowmWallErrInc\%. With one history frame, after the first rollout step the head
transports DINO-WM's own predictions rather than measured features (\cref{app:plugin}); we have not yet tested this
explanation, and the $S{=}1$ ablation in \cref{tab:ablations} addresses it. Content that is not visible in any observed frame can only come from the
correction (\cref{fig:failures}), and motion larger than the window, such as camera ego-motion, lies outside the
transport prior; we removed the rare RT-1 episodes in which the base moves.
\paragraph{Gate versus transport.} The test-time gate knockout shows that the trained model relies on transport on
moving patches (\koMoving\% higher error without it, against \koStatic\% on static patches); it is not a retrained
control, and the controlled comparison is Direct (\droidMovingVsDirect\% lower moving-patch error). On static patches
the mean gate still rises to about 0.6 at $k{=}10$ (\cref{fig:anatomy}c), so static content is also routed through
transport; whether that transport is close to a copy through the identity bias is not yet measured. On static patches, part of the advantage may come from the
gated identity path alone; separating the two requires a gated regression head without transport, which we list as
future work.
\paragraph{Uncertainty.} Under uncertain motion, the MSE-optimal forecast averages over plausible sources
(Proposition~\ref{prop:bayes}). \ours{} averages measured features rather than regressing a mean, but it remains a
deterministic model; stochastic feature forecasters \citep{boduljak2025vfmf,porcher2026flowwm} target multimodal
futures and are complementary.
