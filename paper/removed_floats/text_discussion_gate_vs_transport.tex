% Removed in rewrite 2026-09-25: duplicates Sec. 5.3 (Reliance on transport) and Sec. 5.4 (w=1 ablation).
\paragraph{Gate versus transport.} The retrained controls isolate the effect of transport, as against Direct, identical
except for transport, \ours{} lowers moving-patch error by \droidMovingVsDirect\%, and the $w{=}1$ ablation, a gated head
with correction that cannot move content, loses the entire gain.
%
The test-time gate knockout agrees, raising error by \koMoving\% on moving patches against \koStatic\% on static patches.

