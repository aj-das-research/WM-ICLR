% Removed in curation 2026-09-25: redundant with fig:segments (same kind of decoded examples for DROID and Hamlyn) and the placement table (placement curves). Was input from tables/generated/segments_figs.tex (file left in place).
\begin{figure}[h]
  \centering
  \includegraphics[width=\linewidth]{segments_droid.pdf}
  \caption{\textbf{Decoded forecasts, DROID} (target: robot arm). Each column: that method's own $k{=}10$ forecast of the same window, decoded and segmented; dashed: true mask. Selection rule: \cref{app:segments}.}
  \label{fig:segments-droid}
\end{figure}
\begin{figure}[h]
  \centering
  \includegraphics[width=\linewidth]{segments_openh_hamlyn.pdf}
  \caption{\textbf{Decoded forecasts, Hamlyn} (target: instruments). Each column: that method's own $k{=}10$ forecast of the same window, decoded and segmented; dashed: true mask. Selection rule: \cref{app:segments}.}
  \label{fig:segments-openh-hamlyn}
\end{figure}
