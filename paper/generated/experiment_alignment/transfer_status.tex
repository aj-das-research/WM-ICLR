The IWS single-observation development campaign has completed all 27 task, method and seed runs. Source-bound evaluation receipts support the separately reported transfer results. This changes the input interface; it does not establish the historical context model or an upstream SOTA reproduction. The task matrix and completion gates are in Appendix~\ref{app:experiment-alignment}. Official validation remains separate and requires a locked protocol and checkpoint identities.
