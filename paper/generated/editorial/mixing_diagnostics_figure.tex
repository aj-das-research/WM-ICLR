\begin{figure}[!htb]
\centering
\includegraphics[width=\linewidth]{generated/editorial/mixing_multibenchmark.pdf}
\caption[Measured decoder behavior across four video settings.]{\textbf{Measured decoder behavior of ShiftWM (ours) across four video settings.} Recorded input frames identify the examples. Paired markers compare the raw own-location weight $T_h[j,j]$ with the effective weight $M_h[j,j]$ at the final displayed horizon; curves show effective retention and the mixing coefficient $g_h$. We form $M_h=I-D_h+D_hT_h$, where $D_h=\operatorname{diag}(g_h)$, within each seed before averaging over 16 patches and the displayed seeds. Whiskers and bands are seed minima and maxima, not confidence intervals. DROID uses the median-episode development case from Figure~\ref{fig:editorial-qualitative}; IWS uses fixed training inputs, with the available seed counts shown. DROID steps span five native commands; IWS offsets index stored frames. These are descriptive decoder measurements, not cross-benchmark accuracy comparisons. Increased own-location weight follows from the gate construction and does not establish improved prediction accuracy.}
\label{fig:mixing-diagnostics}
\end{figure}
