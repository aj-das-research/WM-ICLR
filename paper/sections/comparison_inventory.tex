\section{Benchmark coverage and comparison completion}
\label{app:comparison-inventory}
We organize comparisons by task, prediction interface and evaluation population.
Tables~\ref{tab:comparison-coverage} and~\ref{tab:comparison-pending}
separate measured comparisons from external methods that still require matched
evaluation. A training seed or checkpoint is not a distinct method. The current
spatial decoder and the historical context model remain separate studies.

\input{generated/benchmark_scorecards/coverage.tex}

\paragraph{Reading the numerical scorecards.}
Each consolidated scorecard names the method and backbone, uses a fixed population within
each comparison group, and identifies the metric direction and horizon.
Black bold marks the lowest error or highest success point mean within that
group, including a baseline when it wins. Green bold marks a positive gain
against the explicitly named reference; neither style establishes statistical
significance. Paired intervals remain in the accompanying inference tables.
The proposed method is labeled ShiftWM (ours); removing a component defines an
ablation, not a separately proposed algorithm. An exploratory development winner
does not silently replace the selected model or its earlier reported results.

\input{generated/benchmark_scorecards/external_plan.tex}

\paragraph{Additional measurements and completion rules.}
Current spatial-model simulator success, physical errors, failure-censored
interaction cost and full CEM timing are complete for all 2,400 trials.
Predictor-only IWS resources are measured separately; encoder-inclusive latency,
audited FLOPs and total training GPU-hours remain incomplete.
The shared-decoder IWS endpoint study reports five RGB metrics and both
image/feature persistence references for all 36 predictors; clip-level FID/FVD
remain unmeasured. These scores cannot be inferred from feature errors.
RLA-WM backbone access has been requested and its comparison remains pending.
New numerical cells and plots are
populated from completed, source-bound comparisons. Ongoing single-seed diagnostics
do not bypass the full-campaign reporting gate. All unfavorable results and
prespecified conditions remain visible.
