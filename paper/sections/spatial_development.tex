\FloatBarrier
\section{Observation-anchored spatial development study}
\label{app:spatial-development}
This registered architecture study addresses recursive prediction drift while
preserving the locations of visual features. It is a new spatial variant;
it does not preserve the original separate observation/dynamics factorization.
The frozen DINOv2-small encoder produces a $4\times4$ grid of 384-dimensional
features. Training-only channel statistics are shared across all sixteen
positions, so mixtures remain in common feature coordinates. Three observed
grids pass through a shared spatial encoder and the pinned LeWM temporal
predictor. A causal action-prefix GRU supplies commands only through the
requested horizon, and a support-only transition context conditions the trunk
through FiLM. Context remains fixed throughout the forecast.

For horizon $h$, let $Z_0$ be the last actual observed grid, $H_h$ the
horizon state, and $E_0$ the spatial encoding of $Z_0$ before context FiLM.
The transport decoder computes
\begin{align}
 T_h &= \operatorname{softmax}_{\mathrm{row}}\!\left(
   Q(H_h)K(E_0)^\top/\sqrt{96}+4I\right),\\
 \widehat Z_h &= (1-g_h)\odot Z_0+g_h\odot(T_hZ_0)
                  +\tanh(R(H_h)),\quad g_h=\sigma(W_gH_h+b_g).
\end{align}
Every forecast retains the actual anchor instead of recursively replacing it
with predictions. The row-stochastic mixture and unit-bounded innovation
imply the componentwise bound $|\widehat Z_{h,p,c}|\leq
\max_q|Z_{0,q,c}|+1$ in shared normalized coordinates. This is an output
invariant, not an accuracy or stability guarantee. Transport mixes semantic
features; it is not a validated physical image warp or RGB generator.

The five registered arms share the spatial inputs and trunk: Autoregressive
recursively updates the feature history; Anchored additive keeps the actual
observation anchor; Spatial transport (ours) adds mixing and bounded innovation;
Context-off and Action-free disable the corresponding information paths.
Transport begins near persistence (identity score bias 4 and gate bias $-3$),
whereas the zero-initialized additive heads begin at exact persistence.
Active parameter counts differ slightly. These arms test the package-level
decoder effect; they do not separately identify mixing versus bounded innovation.

All fifteen models complete 30 epochs at seeds 0/1/2 with identical AdamW
settings, batch size 128 and ten query steps. Window-weighted all-ten
validation MSE selects checkpoints; final metrics average windows within
episodes, then weight episodes and seeds equally. The evaluation contains 141 episodes from 59 recording sessions and 1631 windows. Neither original nor fresh
held-out test observations enter this development study.

Table~\ref{tab:spatial-native} brings all eight predictors, including the
completed follow-up component arms, into one native/pooled scorecard.
Each coordinate system has its own
normalization; comparisons are made within columns. Table~\ref{tab:spatial-versions-all}
preserves all sixteen registered comparisons in one native/pooled display,
including regressions and intervals containing zero. These intervals are
exploratory and unadjusted for multiple comparisons; stronger claims require
an independently frozen evaluation. Table~\ref{tab:spatial-checkpoints}
records every selected checkpoint and the nonidentical active parameter counts.
\IfFileExists{generated/benchmark_scorecards/droid_spatial.tex}{%
\input{generated/benchmark_scorecards/droid_spatial.tex}}{%
\input{generated/real_video/spatial/native.tex}
\input{generated/real_video/spatial/pooled.tex}}
\input{generated/real_video/spatial/checkpoints.tex}

\IfFileExists{generated/external_dinowm/scorecard.tex}{%
\subsection{Adapted official DINO-WM comparison}
\label{app:external-dinowm}
We reuse the official DINO-WM transformer and action encoder
\citep{zhou2024dinowm}, retaining six layers, sixteen heads of width 64,
an MLP width of 2,048 and 19,412,420 trained parameters. It uses the same
three observed grids, recorded commands, training-only normalization,
training/development windows and scoring coordinates as the spatial study.
The interface adapts official native patches to our sixteen pooled tokens,
omits proprioception and supplies 35-dimensional grouped commands. Thus the
comparison tests an adapted official implementation, with greater predictor
capacity than ShiftWM, under our recorded-video forecasting protocol.

Two objectives remain separate: the original shifted one-step visual loss
supervises three prediction slots, whereas recursive H10 supervises all ten
future grids with full backpropagation through predicted inputs. Each completes
30 epochs at three seeds, using the same batch size and optimizer budget as
our controls. This budget differs from the original DINO-WM recipe. Both select
the earliest minimum of window-weighted FP32 recursive H10 validation error;
final scores equally weight episodes and seeds. No test recording enters the
study. The complete finalizer verifies all six external runs and fifteen
original internal controls before constructing any comparison.

\paragraph{Interpretation and remaining baseline sensitivities.}
Both adapted variants have higher native ten-step error than persistence
(0.355590 and 0.331548 versus 0.226129). The fixed 30-epoch, batch-128,
$10^{-4}$ cosine-decay recipe differs from the pinned upstream defaults of
100 epochs, batch 32 and predictor/action learning rates $5\times10^{-4}$.
The adaptation also channel-standardizes visual inputs before an unchanged
terminal LayerNorm, whereas the upstream visual path consumes raw encoder
embeddings. These are concrete representation and optimization differences,
not demonstrated causes of the observed gap. All six runs completed their
budget; a larger native-representation baseline study is needed before drawing
a broader external-method conclusion.

\input{generated/external_dinowm/scorecard.tex}
\input{generated/external_dinowm/contrasts.tex}
}{}
