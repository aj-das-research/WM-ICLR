\section{Experiment alignment and single-observation transfer}
\label{app:experiment-alignment}
The current proposed method is ShiftWM's bounded observation-anchored spatial
decoder. The DROID development study evaluates its three-observation instantiation. The historical
context model and the single-observation transfer study have different
interfaces and selection protocols; their scores must not be pooled into one
cross-domain superiority claim.

\IfFileExists{generated/experiment_alignment/forecast_transfer.pdf}{}{%
All 27 IWS training runs have completed the full training budget. A common-CPU
evaluation audit is being completed; the numerical study remains pending
complete-campaign finalization. Training completion alone does not populate
the result tables.
}

The study interfaces and metric inventory are indexed in
Table~\ref{tab:comparison-coverage}. The original study contains 27 trained
predictors; the no-$\tanh$ follow-up adds nine. All completed endpoint scores
are collected in Table~\ref{tab:current-iws-scorecard}, with paired comparisons
in Table~\ref{tab:iws-development-complete}. Figure~\ref{fig:iws-forecast-transfer}
combines the complete development forecast curves and paired endpoint gains.
In the original development comparison, bounded ShiftWM reduces endpoint MSE
relative to additive anchoring by an equal-task mean of 1.42\%
(paired 95\% interval [1.15, 1.67]\%). Autoregression has lower endpoint error
than bounded ShiftWM on all three development tasks; that finding remains
part of the study alongside the later no-$\tanh$ ablation.
\IfFileExists{generated/iws_reserved_evidence/scores.tex}{%
The separate reserved comparison appears in
Figure~\ref{fig:iws-reserved-comparisons} and
Table~\ref{tab:iws-reserved-scores}.}{}

\paragraph{Single-observation transfer contract.}
The IWS interface takes one observed image and $H$ recorded native
command rows, then predicts the $H-1$ subsequent stored-frame features.
For $H=60$, the observed index is $s$, the target is $s+59$, and all command
rows $s,\ldots,s+59$ condition the endpoint. These are stored indices, not
verified seconds or sixty physical transitions. The three temporal slots
initially repeat the one observed feature grid; they do not represent three
observed frames. Support-transition context and FiLM are disabled because
one image supplies no observed transition.

The study grid specifies separate per-task checkpoints for autoregression,
additive anchoring and bounded spatial mixing, with three seeds each;
persistence requires no training. PushT, Box and Rope retain their native
command widths of 4, 14 and 8. Internal splits contain 480/120, 481/121 and
481/121 training/development trajectories, respectively. Task-specific
training-only statistics and the full-RGB DINOv2 feature representation are
shared between arms within each task. This is a transfer of the decoder
mechanism, not a shared cross-embodiment checkpoint or an RLA-WM reproduction.

\paragraph{Selection, primary comparison and reserved evaluation.}
The full-horizon recipe trains all 59 offsets and selects checkpoints
by internal-development equal-trajectory endpoint MSE at $H=60$.
The declared primary comparison is bounded mixing versus the additive anchor,
with task-specific standardized feature-MSE reductions and an equal-task
macro of those reductions. Autoregression, persistence, shorter endpoints
and other feature metrics are secondary. The upstream reserved set contains
200 handles from ten trajectories per task; a handle is not an independent
trajectory. Original and derived prefix endpoints remain separately labeled.
The reserved protocol fixes all 36 development-selected checkpoints before
payload access, retaining the original comparison and treating the nine
no-$\tanh$ models as a secondary component family. It uses the exact original
handle order, not the development window stride. The three tasks contain
5,996 native frames in total. Frozen DINOv2 extraction and training-only
normalization are shared by all arms; no statistics are refitted on the
reserved recordings.

For each task, we first average handle errors within each of ten trajectories,
then weight trajectories and three training seeds equally. We also retain
the equal-handle means because handle counts differ between trajectories.
Paired 95\% intervals use 10,000 seed--trajectory bootstrap draws; shared
seed draws preserve pairing across tasks for the macro result.
All 59 full-horizon outputs remain in the ledger. Separate $H=15,30,45$
calls receive only their declared command prefixes, with CPU FP32 prefix
consistency checked at relative tolerance $10^{-5}$ and absolute tolerance
$2\times10^{-5}$. These prefix scores are stored separately, rather than
replacing the full-horizon curves. A complete 36-run finalizer checks the
primitive scores, identical persistence controls and matched input hashes
before any reserved result is reported. No checkpoint, seed or comparator is
chosen from the reserved outcomes.

\paragraph{Numerical execution revision.}
The first reserved execution produced 18 passing predictor ledgers, but two
Box autoregressive seeds failed the declared prefix-consistency check; the
incomplete campaign was stopped. Input-only probes, including the unchanged
scorer with synthetic targets, did not reproduce this failure or establish
its cause. A separately reviewed, post-access revision dispatches the command
GRU one native row at a time. It preserves weights, normalization, examples,
metrics, comparisons and tolerances, and reruns all 36 predictors under this
common execution setting. No original passing ledger is substituted into the
new campaign, and no forecast-accuracy result was used to choose the backend.
This execution change and the original partial artifacts remain recorded
alongside the initial pre-access registration.

\IfFileExists{generated/experiment_alignment/unbounded_component.tex}{%
\paragraph{Innovation-bound follow-up.}
Removing only the correction's $\tanh$ preserves the command interface,
observation memory and mixing pathway. Table~\ref{tab:iws-unbounded-component}
compares all nine new models with the frozen original study, using the same
development trajectories and full 30-epoch budget.
}{}

\IfFileExists{generated/iws_bound_diagnostic/table.tex}{%
\paragraph{Which targets does the range bound exclude?}
A separate post-development audit evaluates the necessary channel envelope
from Equation~\ref{eq:editorial-forecast}. With
$a_c=\min_p Z_{0,p,c}-1$, $b_c=\max_p Z_{0,p,c}+1$ and
$[x]_+=\max(x,0)$, the relaxed squared-error floor for a target $Z$ is
\begin{equation}
 \mathcal L_{\rm box}(Z)=\frac{1}{16\cdot384}
 \sum_{p,c}\left([a_c-Z_{p,c}]_+^2+[Z_{p,c}-b_c]_+^2\right).
 \label{eq:iws-box-floor}
\end{equation}
The box relaxes shared mixing weights and gates across channels, so projection
onto it need not be attainable by the decoder. We retain every development
window and all 59 offsets. Table~\ref{tab:iws-bound-diagnostic} shows that
0.94--1.36\% of endpoint feature coordinates lie outside the envelope.
The nonzero floors identify a representational restriction; they are much
smaller than the observed prediction errors. Channel coupling and optimization
remain unresolved contributors to the no-$\tanh$ improvement. A fixed roundoff
sensitivity changes the displayed floors by less than $1.5\times10^{-7}$;
all-window persistence errors reproduce the frozen ledger exactly.
\input{generated/iws_bound_diagnostic/table.tex}
}{}

\paragraph{Reusable IWS predictors.}
The 27 original predictors and nine unbounded ablations have separate
inference-only bundles containing their exact
weights, task-specific statistics and frozen model code. A relocated, offline
CPU process reproduces every forecast offset exactly on a fixed training input
for each task. This checks portability, not additional forecasting accuracy.
Inputs are one raw 6,144-dimensional DINOv2 feature vector and native commands;
outputs are future feature vectors. The local packages exclude optimizer
state, encoder weights, RGB decoders and future-target inputs. Their complete
manifests and export instructions accompany the source release; checkpoint
uploads remain separate. CPU parity does not certify the CUDA backend.

\paragraph{Result ingestion boundary.}
Only the complete 27-run finalizer can populate the original study's numerical cells. It verifies
the full training budget, checkpoint and source identities, every eligible
window and independently reconstructed metrics. Development and reserved
evaluation use distinct finalizers. Negative and inconclusive results remain
visible; incomplete studies remain pending. The separate innovation-bound
ablation requires all nine new runs and all 27 frozen original runs to pass
its own finalizer before its tables and extended forecast curves are populated.
\IfFileExists{generated/iws_reserved_evidence/scores.tex}{}{%
\input{generated/experiment_alignment/ingestion_status.tex}}
\input{generated/iws_compact_evidence/contrasts.tex}
\IfFileExists{generated/benchmark_scorecards/iws_development.tex}{%
\input{generated/benchmark_scorecards/iws_development.tex}}{%
\IfFileExists{generated/experiment_alignment/secondary_metrics.tex}{%
\input{generated/experiment_alignment/secondary_metrics.tex}}{}}

% Local placement policy only: retain the source-bound table/figure bytes,
% fonts and captions while allowing related appendix evidence beside its text.
\begingroup
\let\iwsoriginaltable\table
\renewcommand{\table}[1][]{\iwsoriginaltable[!htbp]}
\let\iwsoriginalfigure\figure
\renewcommand{\figure}[1][]{\iwsoriginalfigure[!htbp]}
\IfFileExists{generated/iws_reserved_evidence/scores.tex}{%
\input{generated/iws_compact_evidence/figure.tex}
\subsection{Complete reserved comparison}
\label{app:iws-reserved-results}
The scorecard reports all five predictors and four feature metrics on the
reserved population. The following contrast tables retain every declared
comparison, its signed gain and its paired interval. The equal-task macro
averages relative reductions within each task, rather than pooling feature
errors with different task-specific normalization. The standardized-MSE
bounded-versus-additive contrast is primary; other comparisons and metrics are
secondary and unadjusted. Separate command-prefix endpoints and complete
forecast curves remain available in the machine-readable evidence.
\input{generated/iws_reserved_evidence/scores.tex}
\input{generated/iws_reserved_evidence/contrasts_standardized_mse.tex}
\input{generated/iws_reserved_evidence/contrasts_standardized_mae.tex}
\input{generated/iws_reserved_evidence/contrasts_raw_dinov2_l1.tex}
\input{generated/iws_reserved_evidence/contrasts_feature_cosine_distance.tex}
}{}

\paragraph{Measured predictor cost.}
Table~\ref{tab:iws-predictor-resources} adds a separate, completed resource
study of all 36 released local predictors and persistence on fixed training
inputs. Each call returns all 59 forecast offsets. Across tasks, the no-$\tanh$
ablation uses 0.870--0.872 million parameters and takes about 115--116\,ms on
the profiled CPU or 64--65\,ms on the GPU, using the median of three
seed-level medians. Both spatial variants are faster than autoregression in
this measured setting; the additive anchor is faster still. This is a cost
comparison of the exported predictors, excluding the image encoder and
control loop. These measurements use the original full-sequence GRU dispatch
(two CPU threads, or CUDA), not the eight-thread, row-wise dispatch of the
reserved numerical revision. They are not latency measurements of that revised
runtime and supply no CUDA accuracy-equivalence claim.
\input{generated/iws_resources/predictor_resources.tex}
\endgroup
