# Geometry diagnostic figure brief

Scope: development diagnostic of the existing model; not a new successful method
result. Use the installed paper-figure-creation skill. Actual paper width5.5in.
Input: recorded CF2X endpoint images under canonical and warm rendering transforms.
Claim: the measured warm-goal separation contracts substantially, and the current
fixed-context observation FiLM has insufficient expansion range to undo it.
This bound does not prove that a planning failure is caused by the contraction.

Composition choices considered: (1) latent scatter connected to thumbnails;
(2) aligned canonical/warm image rows beside normalized separation bars;
(3) formula-centered operator with separate thumbnails and chart underneath.
Choose (2): the same physical goals remain recognizable across rows and a compact
chart distinguishes the measured effect from the analytic capacity bound. A
scatter would require adding a projection that could itself distort geometry.

Images: three actual development endpoint goals chosen by minimum, median and
maximum recorded X position at gain0.75. Common central80×80 crop [24:104,24:104];
no generated content, removed background or retouching. The warm row uses exactly
the registered pointwise photometric transform. These are goal examples, not
method rollouts or success examples.

Evidence: reports/evidence/extensions_mechanism_probe.json. Use all16goals and
120pairs in the bar chart; display transformer seed0 Framewise and ShiftWM, and
link the complete six-model report. Normalize mean pairwise latent MSE by the
canonical mean. The analytic upper bound is1.21times the uncorrected warm value,
not an achieved performance result or confidence interval. No error bars are
available for this descriptive finite-set summary; do not invent uncertainty.

Outputs: editable Matplotlib source, PDF/SVG/PNG, exact source/image hashes,
caption and visual review. Keep separate from paper until actual-pixel review.
