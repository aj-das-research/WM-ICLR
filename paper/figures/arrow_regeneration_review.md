# Regeneration against the supplied arrow defects

The user's `figure-issues/broken-1.png` and `figure-issues/image.png` identify real defects in the previous version: the feedback arrow's head sat on its horizontal elbow, and the first-block arrow's head consumed its short upward segment. Earlier reviews accepted these incorrectly. This pass checks those exact examples and the same failure mode elsewhere.

Three repair strategies were considered: shrinking arrowheads in the existing layout (would retain disconnected group origins and decorative border crossings); separating context inference and planning into two panels (would repeat several objects and increase tracing); and retaining the visual narrative on a new, shared grid with group ports and wider connection gaps (selected).

The method figure now uses explicit 2.7×2 pt connector tips, roughly 4–5 mm straight approach segments, visible groups around the full history, feature matrices, and executed actions, and a vertically aligned dynamics-context → action-adapter → predictor column. The predictor is one card. The history-return approach is about 4.2 mm before its tip, and the first-block approach about 5.1 mm; these are drawing dimensions, not experimental measurements. Both are several times longer than their 0.95 mm arrowheads. Command-vector glyphs have a separate smaller arrow style.

Root and an independent reviewer inspected the 550px proof and enlarged crops corresponding to both supplied screenshots, then followed every internal connector. Raw-feature → calibration, calibration → corrected features, context → calibration, context → action adapter, candidate actions → adapter, adapter → predictor, and predictor → latent root all now have visible shafts. Group connections attach to visible boundaries. No new line/label overlap or scientific-flow error was observed in this version. The caption still specifies schematic latent futures, fixed contexts within CEM, corrected goal features, five executed native controls, and fixed online weights.

The setup figure was repaired separately: smaller explicitly sized heads, exposed shafts, clearance from the rotated response shape, more space around task labels, and a bottom legend margin. The factor assignments and source images are unchanged. The teaser's actual PDF had a −26.8% / Reacher text collision, now removed and guarded by a rendered-bounding-box check. Other measured plots were reviewed without altering data to separate naturally overlapping curves.

Canonical sources remain `world_method.tex`, `factor_split.tex`, and `../scripts/render_teaser.py`. The screenshot files are unmodified. Exact paper-width/detail/crop proofs are under `paper/build/spacing_rebuild/`; the final manuscript is compiled from the same sources. This review supersedes the previous geometry assessment. It records specific inspected repairs, not an automated guarantee of visual perfection.
