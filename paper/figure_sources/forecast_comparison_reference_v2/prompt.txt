Use case: scientific-educational.
Asset type: COMPOSITION REFERENCE ONLY for a scientific-paper teaser. This raster will be inspected and rebuilt as editable native vector geometry. It is not an experimental result or an authoritative computation graph.

Create a very wide, low-height editorial scientific comparison on a pure white background, approximately 3.7:1 aspect ratio, with two balanced side-by-side panels separated by generous white space. The whole image should feel like a polished Nature/ICLR method illustration: crisp vector-like objects, subtle 3D depth only to explain spatial features and channel structure, elegant thin continuous arrows, restrained shadows, excellent negative space, short regular-weight labels. Use deep navy ink, muted coral for the baseline, saturated teal for ShiftWM, cobalt blue for fixed observations, gold for action-input ports. Do not use dark backgrounds, gradients across the canvas, ornamental frames, or decorative icons.

LEFT PANEL — one autoregressive predictor and one clearly visible output-return feedback loop:
Place a single compact coral predictor module in the center-left with a small gold action port entering from above. To its right, show ONE structured spatial feature grid as the forecast output: a shallow rectangular palette of distinct colored feature tiles with subtle channel-layer depth. A thin coral connector leaves the output and loops continuously underneath the predictor back into its left input, clearly showing that a previous predicted feature state is reused as input. The loop must start at the output, have a visible arrowhead at the predictor INPUT, and not merely circle as a decorative ring. Do not repeat three predictors or three cubes. Short panel title: "Autoregressive". Optional tiny loop label: "Previous prediction". Short module label: "Predictor". Output label: "Forecast".

RIGHT PANEL — persistent observed source with no output feedback:
At the left of this panel show ONE larger fixed observed spatial feature palette, with a visible 4 by 4 front arrangement of distinct cobalt, teal, coral and gold feature pieces and understated channel-layer thickness. Give the source a stable slim cobalt pedestal/underline to communicate retained evidence, not a padlock. Title above it: "Fixed observation". Draw three representative source feature pieces retaining their color identities, each following a clean, purposeful connector into a compact teal weighted-mixing operation. Show a separate small gold/coral bounded-correction cue beside the mixing operation: projected channel bars passing through two short bounding rails, then added at a visible plus junction. The bounded correction must NOT be drawn as taking its input from the mixed output; treat its input as a separate small action-conditioned state port. The mixture plus the independent correction feed ONE future feature grid at the far right. There must be NO arrow returning from this forecast to the observed palette or predictor. Use the title "ShiftWM", short labels "Mix", "Bounded correction", and "Forecast". Make the persistent-source-to-new-feature relation readable without prose.

Scientific constraints: all grids, channels, and colored pieces are symbolic latent visual features, not real or generated RGB photographs, not image patches physically moving in space, and not measured learned activations. No numerical values, plots, error bars, fabricated accuracy claims, fading colors, progressive blur, visual collapse, checkmarks/crosses, or qualitative claims that one output is more accurate. Do not include the nine-dataset gallery, benchmark names, photographs, mathematical equation walls, text paragraphs, or a third repeated-output chain. Preserve equal visual dignity and comparable forecast objects on both sides. The primary visual contrast is the topology: a return loop versus a fixed observed source.
