# Forecast comparison: generated composition reference

`composition_reference.png` is the original, unedited output of the built-in image-generation tool. `prompt.txt` preserves the exact instruction and `generation_manifest.json` records hashes, provenance, pixel review, and required corrections. This raster is a composition reference, not experimental evidence or an authoritative model graph.

The strongest visual idea is one autoregressive output-return loop beside a retained observation palette whose source identities feed one future feature object. All colored objects are symbolic latent features; neither output illustrates measured prediction quality or RGB generation.

For the native vector rebuild, restore action-conditioned state access to mixing and gating, preserve or explicitly collapse the complementary anchor path, and make the projected-feature/bounded-correction relation precise. The generated right-hand action-to-correction connection alone is not the implemented graph. Add observed initialization to the autoregressive path if depicting a complete first-step interface.

The generated PNG is 2072×759 RGBA and has transparency, despite the requested white canvas. Use a white figure background and a wider composition during the editable reconstruction. No accepted figure, shared glyph, frozen scientific source, manuscript include, or dataset asset was modified for this reference.
