# Reserved IWS evidence — presentation brief

The reader should see the signed reserved H60 comparisons, their paired uncertainty, and the distinction between the primary bounded method and the post-development no-tanh component study. The three tasks share the original fixed 200 handles/10 trajectories, three learned seeds, and CPU FP32 scoring. No external-baseline, independent-session, RGB, or physical-control claim is made.

## Three compositions considered

1. Three full forecast panels, five lines each, plus endpoint annotations. This retains temporal shape but duplicates the existing development curve figure and makes five signed comparisons hard to inspect.
2. A task-by-method metric heatmap. This is compact but mixes four different metric units and makes numerical differences and uncertainty less explicit.
3. Three task panels with five aligned horizontal interval rows. This directly answers the registered comparison question, keeps every sign and interval, and uses the primary contrast as the first row. **Selected.** A small equal-task macro summary belongs in the accompanying table, not as a fourth cramped plot.

## Representation and division of information

- Main figure: 5.5 inches wide, at most 2.65 inches high, regular labels at least 8 pt. All 15 task/contrast H60 standardized-MSE points and paired95% intervals; a visible zero line and shared gain scale. The first row is bounded ShiftWM versus additive. The remaining bounded and no-tanh comparisons are secondary. Shapes and direct row labels make color unnecessary for identity.
- Endpoint score table: all three tasks, all five methods, all four H60 feature metrics (60 means). Black bold identifies only the actual lowest point mean, with no significance claim.
- Compact primary-effects table: five contrasts by three tasks plus equal-task macro (20 signed effects and intervals). Companion secondary-metric tables/portable pack retain the other60 signed effects and intervals.
- Source pack: exact completed finalization, all36 per-trajectory metric receipts and source hashes of the locally audited primitive error ledgers, all59 full-H60 curves, separately invoked H15/30/45 values, equal-handle secondary means, and uncertainty provenance.

The plot uses vector marks and editable SVG text. Raster imagery, generated references, and draw.io add no explanatory value for this quantitative comparison. No invented layout numbers are written to manuscript outputs. The renderer must emit nothing while finalization is absent/incomplete; actual-pixel review remains pending until real outcomes exist.
