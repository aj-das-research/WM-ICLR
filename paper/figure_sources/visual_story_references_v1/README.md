# Generated visual composition references

These two images were generated with the built-in `image_gen.imagegen` tool in response to the request for a richer scientific teaser and an exploded architecture illustration. They are **composition references only**, not experimental photographs, scientific diagrams, measured predictions, or numerical evidence. Neither image replaces an accepted manuscript figure.

The exact prompts are in `prompts.json`; generated image dimensions, paths, and SHA-256 hashes are recorded in `generation_manifest.json`. No API fallback was used.

## Teaser reference

`teaser_composition_reference.png` offers a prominent camera/observation motif, layered objects, contrasting recursive and observed-anchor paths, secondary dataset cards, and a quiet evidence band. These are visual design ideas, not a validated computation graph.

Required corrections when using the composition:

- Replace illustrative dataset cards with the actual, source-attributed recorded frames. Any retained generated scene must be explicitly labeled conceptual artwork.
- Connect every anchored forecast to the fixed observed source. Do not copy the apparent forecast-to-forecast connection in the lower blue route.
- Do not interpret progressively warped autoregressive objects as measured, monotonic error growth.
- Restore the action/history qualifier. Populate the evidence band only with the audited DROID development statistics; separate IWS input examples from completed results.

## Architecture reference

`architecture_composition_reference.png` suggests four distinct visual scales: a compact causal backbone, a magnified patch mixer with source/destination identities, a gate/correction mechanism, and an aligned equation rail.

Required corrections when using the composition:

- The generated driving images are not our task. Use the actual authorized DROID observations and preserve the inference/evaluation boundary.
- Reconstruct all scientific wiring from the frozen model and its 28-dependency graph, including past-only context, chronological action prefixes, FiLM, and pre-FiLM anchor keys.
- The bounded innovation is computed from the horizon state through the appropriate normalization, affine map, and tanh. It is added after the complementary anchor/mixed-source blend. The generated raster's apparent blend-to-innovation edge is not correct.
- Typeset the exact equations as editable vector text. A component-wise innovation bound in standardized feature coordinates is not a prediction-error or stability guarantee.
- Do not import historical two-context/CEM/paired-supervision mechanisms into the current spatial architecture.

Final diagram authors should preserve the visual richness while using original editable geometry, source-backed photos, exact mathematics, and an independently checked connector graph. Actual-size, grayscale, and enlarged-port inspection remain required for the final manuscript figures.
