# Camera and observed-frame teaser: composition reference

`composition_reference.png` is an original, unedited output of the built-in image-generation tool. It is a visual composition guide only. The exact prompt and pixel-inspected design findings are preserved in `prompt.txt` and `generation_manifest.json`. No generated scene is experimental evidence.

Transfer the compact rounded camera, recognizable frame stream, retained-source emphasis, lightweight forecast fan and loosely grouped task strip into editable vectors. Use the existing attributed real frames for every dataset image, preserve their content/aspect, and retain the full evidence and scope labels in the final teaser.

The generated AR arrow does not depict feedback; feature counts are not dimensionally correct; and the gold arm must represent supplied commands only. Those are mandatory corrections in the final rebuild, not suggested changes to the scientific model. All final connections, labels and result marks remain native editable elements.
