\textbf{Forecast profiles and matched component gains on DROID development.}
(a) All 50 mean native feature errors for the five original methods; one forecast
step spans five native commands. (b) All six controls at h5/h10: comparator-minus-ShiftWM
MSE with paired 95\% recording-session $\times$ seed bootstrap intervals
(10,000 draws, seed 173). Right of zero favors ShiftWM (ours); open marks include
zero. ``No mix/no bound'' is the additive anchor; ``No mix (bounded)'' adds bounding.
Omitting bounding is slightly better at h5, with both native intervals including
zero. The six component follow-up models were added after the original controls
were revealed. Table~\ref{tab:editorial-spatial} retains all endpoint scores;
Tables~\ref{tab:spatial-versions-all} and~\ref{tab:spatial-versions-components}
retain all 36 contrasts, including coarse-coordinate regressions and interactions.
