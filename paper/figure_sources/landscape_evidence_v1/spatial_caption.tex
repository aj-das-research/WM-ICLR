\textbf{Matched spatial ablations on DROID development.}
All six controls and both native $4\times4$ endpoint horizons are retained.
Points are comparator-minus-ShiftWM MSE; right of zero favors ShiftWM (ours).
Bars are paired 95\% recording-session $\times$ training-seed bootstrap intervals
(10,000 draws, seed 173); open markers include zero. Percentages are relative
error reductions, with favorable point gains in green, not significance claims.
``No mix/no bound'' is the additive anchor; ``No mix (bounded)'' is its bounded
counterpart. The slight h5 disadvantage against no bounding remains visible.
The population is 1,631 windows from 141 episodes, 59 sessions and three seeds;
all 21 models completed 30 epochs, including six post-development follow-ups.
Tables~\ref{tab:spatial-versions-all} and~\ref{tab:spatial-versions-components}
retain all 36 effects, including coarse-coordinate results and interactions.
