# Complete spatial architecture appendix

This reporting layer does not change the frozen spatial experiment. Generate
nothing in the manuscript until the authoritative finalizer passes all fifteen
30-epoch runs and their relocated CPU parity. Reverify the scientific registry,
source/configuration identities, executed configurations, training journals,
selected checkpoint identities, every expected validation window and episode,
and all four ten-horizon metric vectors. Independently recompute equal-episode,
equal-seed aggregate means and sample seed deviations, verify deterministic
persistence across all arms, and recompute all sixteen paired endpoint effects
using a separate seed/session bootstrap implementation (10,000 draws; seed173).
No original/fresh test payloads are opened.

Bind each finalizer relocation proof's package-manifest hash to the actual
inference-only export and require its model/configuration bytes to equal the
selected run package. Record these export identities, training summaries,
journals, and both best/last model/configuration/package manifests in the
paper evidence snapshot; recheck every recorded hash before writing the include.

Report every arm in separate native4x4 and original2x2-coordinate tables,
including persistence. Cross-resolution scores must not be equated. Include
all sixteen paired contrasts and all selected epochs/active parameter counts.
Positive point gains are green and bold; dagger marks an interval including
zero. Every regression remains visible. Tables describe original validation
only, with unadjusted exploratory intervals and no state-of-the-art claim.

Describe the spatial method as a new candidate with one support context, not
the original two-context factorization. Keep approximate parameter matching,
near-versus-exact-persistence initialization and the inability to isolate
mixing from bounded innovation explicit. The componentwise output bound is
an algebraic invariant, not an accuracy/stability theorem. Latent mixing is
not an RGB or physical-warp prediction.

Compile all tables at official ICLR width and reject overflow/reference
warnings before atomic publication of the appendix include. A real visual
review is still required afterward and remains marked pending in evidence.
Keep candidate architecture/qualitative figures outside the manuscript until
actual visual inspection and semantic review. Reporting source, configurations,
checkpoints and ledgers are hashed into generated evidence. Only complete
source-backed results can trigger the background paper build.
