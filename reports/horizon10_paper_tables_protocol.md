# Horizon-ten paper tables: evidence and publication gate

Use the paper-figure-creation skill's evidence rules for these quantitative appendix tables. This is a separate postprocessor; no registered training, evaluation, selection, architecture or protocol source is modified.

Inputs: the completed `reports/real_droid_horizon10_results.json`, immutable `configs/real_video_development/horizon10_v1/registration.json`, all twelve full30 run packages/journals/receipts and all sixty source evaluation JSONs. The job runs only after registered finalizer 200200 succeeds. Reject partial results without writing a manuscript include.

Independently verify all registered source/configuration/data/checkpoint hashes, exact train/validation scope, epoch30 completion, selected checkpoint objective, three seeds for every method, all expected metric keys, and each validation episode/session/start population reconstructed from the immutable manifest. Recompute every episode-weighted source mean and all twenty registered contrasts, including the ten-thousand-draw seed/session paired confidence intervals, using a separate numerical implementation. Repeat CPU weights-only reload parity on one causal original-validation input for every package. No test payload may be read.

Tables retain Framewise, Constant dynamics, ShiftWM (ours), and Action-free. Display both endpoint and mean-all-query metrics on standard h5/h10 populations; paired ours-versus-Framewise intervals; original-h5 versus new-h10 predictions at exactly matched h10 windows and their h5 prefixes; selected epochs by seeds0/1/2; parameter counts; measured invocation duration. H10 training and matching model-selection horizon are the control treatment; original window weighting is preserved. Equal-episode selection journal values are auxiliary only. The ten-step all-query mean is primary, other endpoints/horizons secondary. Retain unfavorable results and intervals crossing zero. Green bold marks positive relative-percentage estimates, never statistical significance; a dagger marks intervals including zero. Units are standardized feature MSE; gains are relative percentages, not percentage points.

The native five-step and ten-step populations can differ in complete-window eligibility. In matched training-horizon comparisons, exact episode IDs, session IDs and query starts must agree. Report validation results as development evidence, with unadjusted exploratory intervals; do not imply independent-test confirmation or extra architectural novelty. Runtime includes the successful invocation's training, five evaluation passes and offline reload; prior interrupted work is excluded and hardware differs, so it is not a controlled speed comparison.

Compile all generated tables twice with the actual official ICLR2027 style at native text width. Reject overfull boxes and undefined or duplicate references. Preserve the proof PDF, TeX, complete logs, evidence ledger and exact source SHA-256 values. Mark visual inspection pending honestly; a TeX gate alone is not a visual review. Write `paper/sections/horizon10_development.tex` last, only after all numeric/provenance/proof gates pass; then compile the integrated manuscript through its existing locked build script. Root adds the conditional include. No Git push or external publication is performed by this postprocessor.
